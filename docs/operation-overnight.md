# 2026-09-21 主力持续控制过夜实验

**已于2026-09-22 08:02完成。** 61,216局、0错误；26完整轮加一轮主线。持续控制有有限正向收益，旧窄控制候选仍强，默认玩家不变。最终表与回放见本文末尾收取记录。

用户授权今天17:30下班、明早09:00收取；服务器必须脱离开发机独立运行。稳定玩家0.1.16保持。先闭合可执行教师，再做模仿学习对照和两路线持续演化；不另开泛化预研。

## 当前干预

保留原`teacher`作原始规则对照；新增`teacher-menu`（当前menu-v2，早期menu-v1证据见下），直接选择并执行operation-v2中的一个合法动作，真实订单类型决定含义。撤回成员到达后仍可集结/再次出击，不等待释放；部分召回明确改成整军回防。复用已有作战风险估计、成组规则和补兵阈值，不修改动作语法、输入、奖励、生产与局部战术。该教师是有明确改写的规则策略，不声称原规则无损编码。

新增反例覆盖途中撤回、到达再行动、集结后出击、部分召回的合法替代、守备来向改变，以及教师动作真实走策略执行器。最早两场实际接入局主线/压力均正常胜Supalosa，135/172条记录全部为实际合法动作，不用来声称实力改善。之后修正防守锚点更新，正式采样以冻结源码为准。

## 实验次序与边界

1. 主线/压力的新旧教师各128完整局，四张已见图、四个固定对手、相同数据预算；47/83各自完整局划分、20轮BC。先比较教师和新旧BC，核对关键撤回/到达/再行动事件。
2. 保留已有效的窄权限候选作为持续演化路线；完整权限从可执行教师BC起点开始。主线、压力交替对冻结新对手采样，两种子分别保留BC起点与较早候选，不能把最新更新自动当最佳。
3. 在可用CPU/内存范围测量128/160/192并发的相同工作块，按整块ticks/s与峰值内存选择；不以worker数本身作为优化结果。训练任务可按独立模型并行。
4. 训练最迟2026-09-22 07:45 +08:00不再开始新阶段，为09:00前最终比较预留时间。按固定游戏预算和时间双重边界；正常未决按U/效用0，真实错误保留并停止相关批次。每阶段保存可恢复检查点；重新启动先恢复已完成状态，再判断截止，不允许截止后退回初始候选。
5. 完成批次无损压缩日志，保留检查点、回放、失败证据和50 GiB空间余量。结束后保持原试玩版，次日根据实际完整成绩决定是否晋升。

具体上限、冻结源、实际预检和启动证据在执行后补记。没有把尚未运行的计划记为通过。


## 首份128局预检与修正

`d0fae93`的四图四对手预检：新教师主线23/32、压力21/32胜；旧教师21/32、18/32，零错误。新教师22次撤回到达均随后集结，旧教师68个到达点均出现不可表示的动作。这尚不证明新的BC或PPO更强。

同时从真实指令切片发现，新教师在没有新兵时会改到备用集结点，下一拍又改回来。已修正为：同类型订单只有补兵、目标受阻或新锚点确实更贴近当前需求时才改令；现有合适的集结继续KEEP。新增反例并复验；最早128局不冒充修正后的正式数据。

过夜工作上限为80,000局、40个交替轮次（还受07:45训练截止限制），最终比较预留4,096局并保留09:00硬截止。正式每路线/权限两种子；每轮每模型192新对局，开发选择每模型48局；最终同池每模型96局，精确相同模型SHA合并，保留较早最佳检查点。窄权限从R2保留候选继续；其起点与新完整权限不同，不能将二者差异称作纯接口因果效应。新旧教师BC使用相同128局/20轮预算，单独比较。

状态文件先记录已选模型与路线游标，恢复时直接读取，再检查截止。训练并行按独立模型执行；每阶段有独立日志、完成标记和模型概率校验。`STOP_TRAINING`文件可让任务在阶段边界转入最终比较。校准与完整过夜流程的实际验收和启动状态待补记。


## Pro代码审查后的两项修正

Pro直接读取了固定`7dc0f76`及相关源码；本地核实两项具体反例。训练与概率校验完成但开发比较未结束时，模型原先未写进state.current，截止转最终比较会漏掉它。现逐份持久化BC和pendingUpdated的路径/SHA，最终比较纳入已校验pending但不自动晋升；开发选择完成后再提交current/retained/游标。新增重启前后pending进入最终比较、保留旧incumbent及哈希变动拒绝的检查。

menu-v1的“全体到达、全体成组”可能让一名长期掉队者锁住主力。menu-v2设置撤回等待上限900 tick（60游戏秒）后转合法集合，并按至少占2/3且至少两辆装甲的成组核心评估出击机会；掉队成员仍保留授权、接受同一任务，不加RELEASE或改模型掩码。构造的7车已返、1车长期远离反例已走通集合→再出击且8个成员均保留。它证明这一反例修复，不证明所有地图上都有路径可达。

窄权限起点逐份与上一轮审计SHA核对：主线47 PPO0和压力83 PPO1为原路线incumbent；另两份是种子83/47各自的最新端点，明确作独立种子延续，不把四份都称为上一轮冠军。

`e814119`的初始化已在512场教师完成后主动停止，尚未进入正式BC：menu-v1主线83/43/2、压力77/50/1；旧教师89/37/2、68/58/2（W/L/U，E全0）。保存为menu-v1证据，不能混入menu-v2的新数据。更新后的正式批次将使用新输出目录。


## 完整管线和恢复检查

`dda5f7c`的88局端到端预检已完整结束，59正常终局、29预定上限、0错误，逐局结果/角色/源码/模型与汇总一致；包含两路线的BC、PPO、概率校验、冻结对手和最终比较。此预检只有两局教师和一轮优化，不能用作强度评价。对完成目录的独立副本触发训练边界后恢复：current/retained均保留已完成更新，未退回初始化，未启动新训练，复用相同最终比较计划。新增pending更新的持久化/最终纳入另由Python反例验证。170项Node、18项Python和类型检查通过。

menu-v2正式初始化的冻结源为`bf105ab`，新目录与先前试验分开。512教师局全部完成：主线新教师76/49/3，旧教师91/35/2；压力新教师88/39/1，旧教师75/49/4，E均0。新教师101次撤回到达中记录到73次同编组再次出击，其余原编组在再次出击前失效；不能把合法动作覆盖率代替这项连续行为核对，也不能据此宣称新教师普遍更强。

八份BC按各自128局/20轮及两种子完成，跨运行时概率校验通过。主线初始同池每份64局：menu47为45/17/2、menu83为43/20/1；旧教师BC分别42/18/4、36/23/5；规则0.1.16为43/20/1，E均0。窄权限两个延续起点均40胜。该比较仅四个固定对手、四张已见图，出生不配对，不能与上一轮六对手池的96局百分比直接相减。压力初始化及夜间实际放行/并发选择将据真实状态补记。


## 已放行独立过夜执行

正式初始化共1,408局完成、E为0。压力初始同池每模型64局：menu47为35/27/2、menu83为40/23/1；旧教师BC为41/21/2、43/19/2；窄权限起点44/18/2、37/26/1；规则0.1.16为44/18/2。新教师的收益有路线差异，不能称为普遍改进。保留全部旧BC、两种子起点、当前学习者及较早incumbent，继续用相同完整局奖励开展有界PPO。

已检查八份BC的SHA与概率校验、两路线完整初始结果和连续行为记录，并写入服务器的`ADMIT_LEARNING`收据。后续独立SSH重连已看到任务自动进入并发校准；本机不再是后续阶段的前置条件。源码`bf105ab`，任务定位在忽略文件`work/server-feasibility/active-job.json`的`operationOvernight`。校准开始时没有其他Warbook采样批次，128/160/192三档完成后自动按ticks/s及180 GiB峰值限制选择。当前仍在校准，不先声称192路更快。

07:45训练边界、09:00硬截止、80,000局/40轮上限均在服务器执行；截止时保留有效pending模型进入最终比较。静态CLI任务不会自动重启崩溃进程，失败会保留状态/日志和已验证候选。默认玩家0.1.16不变，不按开发筛选自动晋升。

收尾检查另发现，旧批处理重启一个没有完成标记的目录会追加原决策日志。开发分支已增加独立attempt目录，保留中断记录而不把两局拼进一个训练episode，并有回归检查。该数据恢复改动未覆盖正在运行的冻结`bf105ab`任务；如以后要在截止前人工重启该旧任务，必须先隔离无`batch-row.json`的中断目录，不能直接对其追加。当前没有发生此类重启，也没有自动重启器。

离开前17:28重连：128路512局用324.18秒、34,206.5 ticks/s、峰值约106.7 GiB；160路512局用321.58秒、34,689.4 ticks/s、约134.7 GiB。差距很小，未宣称明显提速；192路已自动启动，最后按完整测量选档。两次SSH断开后进程持续推进，仅该过夜任务和原有无关warmup会话在服务器运行。


## 2026-09-22 收取：整夜完成，持续控制有有限正向收益

本次独立任务正常结束，退出码0，无中途失败。07:34:36完成最后一轮主线开发比较，因下一阶段预计越过07:45训练边界而转收尾；08:02:37完成两路线最终比较。实际26个完整双路线轮次，主线再多一轮：每份主线模型27次更新、5,184场新增RL对局；每份压力模型26次、4,992场。训练与评估全程保持冻结行为源`bf105ab`。

逐局复算**61,216局**：58,910正常终局、2,297预定tick上限、9引擎无错误双方败北未决，**0程序错误**。源码、模型SHA、角色和汇总均一致；220份新训练模型各自的训练/验证整局分组无交叉，概率导出校验通过。八条学习轨迹的actor权重均实际变化，不能把文件元数据变化冒充训练。没有重启中断对局或拼接日志。

局数拆分：教师512、初始比较896、并发校准1,536、RL采样40,704、开发筛选15,264、最终比较2,304。前期预检与被中止的menu-v1数据另列，不混入61,216。没有把6万总局数说成单个模型的训练量。

### 同池最终结果

每份模型96局，以下为W/L/U，E全0。“初始”与“末次”都在最终冻结池重新跑；持续控制的初始为menu-v2 BC，窄控制的初始是上一轮的PPO端点。

| 路线 | 权限 | 种子 | 初始 W/L/U | 末次 W/L/U | 胜场变化 |
|---|---|---:|---|---|---:|
| 主线 | 持续控制 | 47 | 58 / 34 / 4 | 58 / 31 / 7 | +0 |
| 主线 | 持续控制 | 83 | 60 / 34 / 2 | 67 / 28 / 1 | +7 |
| 主线 | 窄控制 | 47 | 53 / 39 / 4 | 53 / 35 / 8 | +0 |
| 主线 | 窄控制 | 83 | 72 / 22 / 2 | 59 / 31 / 6 | -13 |
| 压力 | 持续控制 | 47 | 49 / 45 / 2 | 56 / 38 / 2 | +7 |
| 压力 | 持续控制 | 83 | 51 / 42 / 3 | 57 / 37 / 2 | +6 |
| 压力 | 窄控制 | 47 | 57 / 37 / 2 | 50 / 29 / 17 | -7 |
| 压力 | 窄控制 | 83 | 54 / 36 / 6 | 63 / 27 / 6 | +9 |

规则0.1.16主线为**55/38/3**，压力为**49/45/2**。原始规则教师的完整权限BC对照：主线47/83为51/33/12、50/28/18；压力为58/34/4、57/37/2。教师语法对齐改善主线起点，但压力起点仍弱于旧教师BC；压力PPO基本追近旧BC水平，不能说所有路线都获益。

最终池固定四个对手为0.1.16主线、0.1.16压力、历史阵地`b3a1f7c`和Supalosa；再加入对方路线开发期保留的窄/完整控制模型。主线面对压力窄cycle23/83、压力完整cycle25/83；压力面对主线窄cycle24/83、主线完整cycle26/47。四张已见图，每个对手16局。路线内同池；两路线适应对手不同，出生不配对，不能跨路线直接排名，也没有未见地图泛化结论。

### 决定下一步的发现

- 持续控制四个重复为0、+7、+7、+6胜；出现正向学习信号，但本轮没有证明收敛或稳定优势。主线83为67/96，比同池规则55/96高；四个固定对手从46/64增至52/64，两个学习对手从14/32增至15/32，收益并非来自Supalosa（始终16/16）。
- 本次最高的主线成绩仍是**旧窄控制83起点72/96**；同一模型继续训练后只有59/96。主线开发期保留的窄模型为63/96，压力开发期保留的窄模型58/96，也低于其末次83的63/96。主线持续控制开发期选47，但最终47/83分别58/67。保留/筛选不总能选出最终池最强者；评估噪声、对手变化和遗忘都可能参与，尚未做因果区分。
- 下一步优先复验保留下来的互补候选，并让初始化/历史强候选继续参加必要的比较，避免单向丢失能力；同时处理下面实战切片的接敌问题。继续训练不是自动升级理由，本次不切换默认试玩版。

### 回放与行为核对

维护入口`/replays`新增主线持续控制83（cycle26）的两份时长居中样本：对0.1.16胜局28:56，以及对学习型窄控制压力对手的败局30:26。两份HTTP回放SHA与原件一致，重放共238/238原单位快照、停止tick、资金和败北状态匹配。原始回放、模型与完整审计留在私有目录；本机定位`work/operation-overnight/harvest/`和`runs/learning-review/night-main-*`。

败局不是“从未出击”：11:00已有10坦克进攻。computer use查看11:40/12:20，前者红方坦克沿岸纵向接敌、后排与接触点有距离，后者残余坦克在更靠后位置横向展开。原决策记录确认11:40仍10成员、平均94%血量，12:20剩5成员、平均91%，宏观订单仍为advance；19:20才改defend，19:40剩1成员、10%血量。整场没有对敌方非防御建筑的损伤记录。这支持继续检查接敌展开、损失后的判断和增援方式，不证明任一替代动作必胜；不能将KEEP或没有withdraw动作等同于没有局部运动。临时回放已正常退出并关闭。

### 资源与归档

自动选择192路：512局校准316.19秒、35,465 ticks/s，约5,829局/小时，峰值158.6 GiB；128/160路分别34,207/34,689 ticks/s，差异有限，不能把校准吞吐当整夜恒定速度。全部61,216份决策日志自动无损压缩，**381.98 GiB→35.94 GiB**；收取时服务器可用364 GiB。过夜会话和游戏进程已结束，仅原有无关warmup保留。稳定玩家仍0.1.16，维护进程PID27573。
