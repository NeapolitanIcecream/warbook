# 2026-09-15 执行进展

## 后续：一键观看 AI 对局

- 按用户的观战请求，首页新增「观看 AI 对局回放」，固定入口为 `http://127.0.0.1:8642/watch`。这是完整真实对局的回放；玩家对战入口仍独立保留。
- 本机选择 `runs/final-clean-check/result.json`：Warbook 0.1.4 对 Supalosa，美国小镇，12:00，Warbook 正常获胜。文件 SHA-256 为 `05f436b9de21e3bef185e2c3270735fda9362536f5d23dbfb73740a47a7fbcf3`；文件与选择配置均未进入 Git。
- 重放脚本再次完成整局，tick=10804、双方资金及败北状态与原结果匹配。图形客户端从首页点击进入后播放到结尾，Game.onEnd 为 tick=10803、Warbook 存活、Supalosa 败北；该事件发生于 Node 最后一次 update 返回之前。
- 验证了全屏、回放加速到 8x、从头播放和 Esc 菜单暂停。状态提示上移，避免遮住播放按钮。类型检查、HTTP 回放哈希、未配置回放时仍可进入玩家对战均通过。
- 只改观战入口与提示，没有改变 AI 策略。当前 UI 包 SHA-256 为 `7cc3071b321bcfad0d8ab7a230596ecf5412a74ec4ecc8d183011bcee1f28327`，代码提交 `2b22679`。主服务使用离线缓存，完成的测试实例与临时服务已关闭，仅保留维护入口和交给用户的观战窗口。

## 最终交付：0.1.4

- 本期从 14:09 开始，截止 17:30。正式入口为 `http://127.0.0.1:8642/`，只在本机监听；首次资源准备后使用 `OFFLINE=1`。
- 0.1.4 已在对齐客户端中完成真实开局、玩家鼠标/键盘展开基地、AI 建设和交战、8:08 正常结算、再次开局和主动退出。播放器输入与 Node 回放仿真是不同证据，均已分别验证。
- 0.1.3 三图每版本各 12 场：候选 36 胜；初始对照 6 胜、28 负、2 未决。这些都是开发数据，不宣称正式目标分布或真人水平验收。
- 官方 0.84 对手构建的城市图曾 3 场未决。根因是飞行兵 `JUMPJET` 的对象类型仍为 Infantry，旧策略误把它当碾压目标。0.1.4 加入可见空中状态和己方防空能力，并按需生产 IFV；`anti-air-urban-check` 6/6 正常获胜，另三图共 12 场回归全胜。裁判记录到 IFV 用 HoverMissile 击毁飞行兵。
- **版本实测更正**：API 0.79.0 内嵌的源码版本常量为 0.83.3，回放兼容标识为 0.83。早期记录中的“引擎 0.84”来自静态 changelog 映射。旧结果仍是 API 0.79.0 上的实际结果，不能按错误标签解释。
- 玩家端采用新下载的官方 `/old/v0.83.3/` 归档，并直接复用 SDK 自带的 `ra2cd.mix`，SHA-256 为 `0c359a661001b463fc29d4b4596ba8054a4cc2d05d92a90fedf6d71dff6162a8`。没有改回放头、跳过校验或复用废弃本机代码。
- 原始回放已在图形客户端完整播放。`runs/frontier-town-check/000-combined/browser-replay-check.json` 验证相同 Game.onEnd 边界：两边 tick=10595、资金 1257/570、败北状态完全相同。Node update 返回后为 tick=10596，这是结束事件之后的最后一步。
- 最终 AI 包 SHA-256：`a32e96b9e68308a36b02230559645e9cc17b00453b7f3f32793b0d067d4353ba`。脚本 `scripts/Play Warbook.command` 可恢复入口服务。构建与服务在启动时固定包，不中途替换已有对局。
- 已采用 `finally` 释放 Node 游戏实例；不用的图形实例、候选服务和实验 worktree 及时关闭/移除。运行证据保留在忽略目录 `runs/`，不会上传游戏资源、密钥或大型产物。
- 当前是规则策略研发版，没有训练模型。海军、全部特殊能力和更多地图条件尚未覆盖；未提供可选的 `theme.mix` 音乐包。

下文保留过程记录；其中“当前”“尚未完成”均以本节最终状态为准。

- 启动：14:09 Asia/Shanghai；本期截止：17:30。用户随后明确不复用被放弃的本机版本，建议完全本地开发。
- Git：`codex/real-games-player-entry`，远端 `NeapolitanIcecream/warbook`。已保留远端历史，未覆盖 main。
- 新建 TypeScript 工程。固定官方 API 0.79.0 / 引擎 0.84；npm 锁文件；Hono 提供本地入口，esbuild 打包浏览器策略。同一个新策略用于 Node 和官方图形客户端。
- 本地原版 MIX 已复制到忽略目录 `assets/ra2`，无需依赖旧项目运行。没有读取/继承其 AI 代码。所有运行产物在 `runs/`，官方客户端缓存在 `work/client-cache/`。

## 已运行证据（截至 14:30）

| 运行 | 结果 | 证据范围 |
| --- | --- | --- |
| `runs/first-engine-run` | tick 0 的无目标 Deploy 触发异常 | 真引擎机制失败；修为 DeploySelected |
| `runs/second-engine-run` | 15,524 tick，蓝方 defeated，2.64 秒 | 起始军队交战；尚未可靠识别结束状态 |
| `runs/first-full-economy` | 9,652 tick，红方存活，2.66 秒 | 已完成经济/生产/交战；旧结束规则保留未完全确认标签 |
| `runs/verified-end` | 13,012 tick，正常结束，2.82 秒 | 无起始军队，双方经济生产；引擎状态 Ended、turn-manager error=false、单方败北 |
| 同局官方 replay 重放 | 13,012 tick，败北状态与资金逐方相同，2.35 秒 | 真实回放仿真；不是仅解析 |
| `runs/termination-limit` | tick 30，Started，无败北 | 截断正确保留未决，没有算作胜负 |
| `runs/supalosa-first` | 9,284 tick 正常负于固定 Supalosa，3.08 秒 | 原生外部对手条件；首场诊断，不作胜率/泛化声明 |

以上单局秒数为具体运行的端到端墙钟，含初始化、资源哈希和记录；不是稳定吞吐估计。前期运行带未提交工作区，后续在提交后继续固定批次。资源/依赖哈希、起点、动作、停止状态和 replay 路径均在每局产物中。

## 信息与结束边界

策略只接收普通数据，不接触 GameApi、全局事件或原生 ID。敌方只来自本方 shroud 查询；离开当前接触后只保留位置。建筑预检仅查询本基地周边、整个 footprint 已探索的候选；协议标为 `api-shroud-v0-visible-placement`，不宣称人类等价公平竞赛。

`src/engine-diagnostics.mjs` 对官方 bundle 校验 SHA-256 后追加只读结束状态导出，不改引擎方法/状态/时序。读取 GameStatus 与 turn-manager error；仅 runner 使用。结果同时保留公开败北观察。回放使用未追加此诊断导出的官方模块重放。

## 玩家入口与当前工作

- 服务：`npm run play`，127.0.0.1:8642。仅本机监听。本地配置、客户端缓存、MIX 与遥测不进入 Git。
- 玩家入口 → 官方客户端本地副本 → 原有 SkirmishScreen → 新 WarbookBot。原版图形/输入/战斗逻辑不重写；只在本地替换 BotFactory，并设置首局默认 Americans、mp03t4、零起始军队。
- 已实际开局，通过键盘 N、D 展开了玩家基地；AI 在实时对局中建设。退出后重开仍在继续验证，不提前记作完整可玩验收。
- 发现 worker 响应缺少 COEP 导致约 10 秒超时，已补响应头，正在复测。首次本地资源自动导入已通过。
- 下一步：完成玩家控制/退出/重开与离线缓存验证；固定可玩版本；根据 Supalosa 首场时间线比较早期军力/经济分配；检查更多真实对局。

6–12 小时校准窗口尚未达到，不虚构 24/72 小时吞吐预测。当前本期以 17:30 为边界交付已验证版本和未完成事项。

## 14:36：第一轮具体修复

- 固定批次 `runs/screening-initial` 先完成 4 场：baseline 两场均 54,000 tick 截断；cohesive 一负、一截断。第五场由 Agent 中断，以修复已定位的问题；没有将截断算成平局或丢弃。
- 截断局保留了 92 辆坦克，但全军一直往最后接触点 (75,28) 下令，已经到达且没有敌军，仍未恢复侦察。原因是历史接触点从不失效。
- 0.1.1 在合法己方单位重新检查了空的接触地点后清除该调查目标，再探索候选出生点；不把目标消失认定为死亡。加入对应回归测试。
- 首轮没有支持“集合更强”的有效结论，先修共同的搜索错误，再以新版本重跑。
- `runs/supalosa-first` 的回放也精确匹配终局；时间线显示对手在 tick 4,500 已有 20 GI，我方为 6 GI、2 矿车且无坦克。下一轮关注步兵集结和早期防守。

## 15:05 检查点与续接位置

**可玩链路已经通过**：本地入口 → 自动导入资源 → Americans / 美国小镇 → 鼠标选择 MCV、D 展开 → 点击生产电厂并放置 → Warbook 摧毁玩家基地 → 正常显示 9:44 结算 → 继续 → 重开。另验证了 Esc → 放弃任务 → 退出 → 结算 → 继续 → 再开。新局保持美国、速度 4、零起始军队。该玩家局用于功能验证，玩家只做了建造操作，不构成竞技水平证据。

- 127.0.0.1:8642 正在 `OFFLINE=1` 下服务。资源缓存完成后重载、实际开局和退出/重开都已执行，无需外网下载。首次缺缓存仍需从官方来源准备资源。
- 试玩固定为已验证的 0.1.1 baseline；bundle SHA-256 `6ce3d99318b0270cd320ca5672a2f1b70541eab937cef154f5a0617b9cca88f7`。启动时读取并固定内容哈希目录，后续编译候选不会替换现有服务。当前固定包 ID 也在忽略文件 `work/stable-player-release.txt`。
- 维护入口：`scripts/Play Warbook.command` 可双击。后台服务的当前运行命令为 `PLAYER_RELEASE=<上述哈希> OFFLINE=1 node --env-file-if-exists=.env --import tsx src/player/server.ts`。
- player 遥测在 `runs/player/browser.ndjson`。整局与主动退出分开记录；初版结算遗漏败北者的问题已修为捕获初始 roster。主测试浏览器是 Codex in-app 的 tab 1，`http://127.0.0.1:8642/game/`，当前为重复开局测试。

### 已完成的小批比较（全部是开发证据）

| 批次 | 基线 | 候选 | 异常/未决 |
| --- | --- | --- | --- |
| `scouting-fixed`，12 场，41.44 秒 | 2/6 胜 | cohesive 1/6 胜 | 全部正常结束 |
| `guarded-screening`，16 场，67.83 秒 | 3/8 胜 | guarded 初版 8/8 胜 | 全部正常结束 |
| `guarded-recheck`，24 场，96.64 秒 | 4/12 胜 | 修正部署 GI 控制后 6/12 胜 | 全部正常结束 |
| `defense-mechanisms`，40 场，161.83 秒 | 6/10 胜 | pillbox 2/10，sentry 6/10，guarded 4/10 | guarded 有 1 场 tick 上限未决，其余 39 场正常结束 |

最初的 8/8 没有稳定复现，**未晋升防守候选，也未宣布胜率提升**。驻防与收起需要区分明确任务；所有这些地图/对手/样本已用于开发，不能再称留出。采样采用普通未控种子的新局，没有假造配对出生点。

### 正在推进

1. 0.1.2 修复建造场丢失后误切换阵营的问题，增加请求→合法观察效果关联；移动只记位置变化，目标消失不记击杀。3 张真实地图 `mp03t4`、`mp06t2`、`mp29u2` 均跑到正常终局，当前基线均负于 Supalosa。尚未替换固定试玩包。
2. 首场碾压候选 `runs/crush-referee-fixed` 在 10,404 tick 正常胜出，提交 70 条 ForceMove；裁判记录了 21 个 E1 被 MTNK 在无武器名称的攻击来源下摧毁，另有炮击事件。该记录与碾压机制相符，仍需完整新局比较。
3. 正在隔离 Git worktree `work/eval-crush`（提交 `1fcacb9`）运行 16 轮 baseline/crush，产物 `runs/crush-fixed-screening`。不用主工作区后续编辑改变批次代码。较早的隔离 worktree `work/eval-defense` 停在 `f5ec832`，该批已完成。
4. 只读裁判曾把 scenery 的 Spawn 当成单位读取，导致 `crush-first` 和 `crush-screening` 24 次启动在 tick 1674 失败。这些失败全部保留，不能算作策略胜负；已改为通用对象查询，并让批次遇到进程错误即停止。固定后的真实首局已经跑通。
5. 缺资源启动也会写 `initialization_error` 结果，已实际测试；run ID 增加随机 UUID 片段避免并行启动时间戳碰撞。记录元数据不会输入策略。

剩余优先级：完成 crush 新局比较，必要时针对早期出兵再做一个小干预；验证 0.1.2 图形候选和可复现资源说明；更新 Git 与玩家固定版本，17:30 前收尾。当前没有 goal 或后台研发自动化，仅当前 Agent 回合与上述本地进程。
