# 2026-09-24：完整战略的纠错与小步强化学习

本轮承接[计划协调实验](commander-coordination-cycle.md)，固定源`0288f66`，13:39已在服务器独立启动。稳定玩家仍0.1.16。本页记录当前试跑；假期长任务尚未部署，不把计划轮数当作已完成。

## 为什么选这两条路

主线新增纯学生访问状态的教师纠错后，两个贪心候选6/32、9/32，继续旧数据仅0/32、1/32。进一步192场校准每项32局：主线47在T=.05/.01/贪心为3/6/4胜，83为6/7/3胜；波动仍明显，不能依据其中最高一次定版。选T=.01作为下一轮固定部署分布，BC统一在T=1训练，分别记录两种温度。

压力同一192局、同一更新序列的576场新比较，每项64局：

| 更新步数 | 种子47 | 种子83 |
|---|---:|---:|
| 未更新起点 | 29（共享控制） | 29（共享控制） |
| 8 | 33 | 34 |
| 16 | 32 | 26 |
| 32 | 30 | 31 |
| 64 | 27 | 26 |

8步两候选在Supalosa/对方0.1.16规则/强旧MC83上都合计19/48胜，原起点14/48；其余胜利来自冻结同代。47的三控制分布为6/16、9/16、4/16；83为9/16、8/16、2/16，起点为6/16、3/16、5/16。对强旧模型仍有退化，不能称为普遍改进或已发现新打法。

两个64步重跑的张量与原实验逐项相同，记录中间检查点没有改变训练。原控制器因独立的主线低温数值预检错误最终退出1；压力576局已单独完成、0E，不能随根状态一起漏报。后续修订数值规则的主线192局亦已完成、0E；原十二份温度模型同步复查通过。所有地图已见，起点未精确配对。

## 六条线路与共同演进

| 线路 | 起点与采样 | 每轮更新 |
|---|---|---|
| 主线47、83 | 各自纠错后的完整模型，T=.01；β=0查询本路线教师，完全由模型执行 | 128份旧锚点＋最近两批实际状态；BC T=1、600更新，再导出T=.01 |
| 压力short 47、83 | 相同压力T=.2起点；纯模型结果 | PPO 8更新，学习率1e-4 |
| 压力small 47、83 | 与short相同起点；纯模型结果 | PPO 64更新，学习率1.25e-5 |

压力四线首轮共享已验证的192局，隔离更新方法；不能计成768份新对局。后续各自采192份新on-policy完整局，主线每轮采128局。主线两起点来自不同优化结果，不称为同起点重复；压力同种子两方法才是同起点对照。

仍使用相同的完整v2战略权限、317千左右参数与固定战术，未添加规则补救、成员禁令或法国打法示范。每个训练进程16 ranks×local batch 2，global batch 32；最多120场同时运行、96个训练rank，按阶段使用资源。

每轮冻结本轮对手，再开局：Supalosa、对方0.1.16规则、强旧MC83和对方正在演进的完整模型。主线交替面对同种子的两种压力更新路线；压力面对对应主线。冻结模型对手使用贪心执行，采样actor保持表中温度。每轮更换采样随机流，但引擎出生仍不可精确控制。

每个候选与本轮保留模型各评64局。保留策略先比较三个固定控制的胜场，再比较全部胜场，避免弱同代独占资格；两者都在同一新矩阵下重评。学习中的current继续接受有界更新，retained单独保存，避免把单轮有限样本的波动当作必须中断学习的理由。玩家默认不会自动切换。

结束时另做冻结Supalosa/两种0.1.16规则/强历史比较，以及主线与压力保留模型的交叉对阵。失败按线路隔离；错误不混作正常败局。更新、比较未结束的候选与真正保留版本分别记收据及SHA。

## 当前试跑的实际状态与边界

任务`jobs/commander-joint-20260924`，tmux`warbook-joint-24`，私有定位在`work/server-feasibility/active-job.json.commanderJointPilot`。六份初始化已通过；首轮short两模型与之前8步模型张量精确一致；small两模型也已完成64步与导出核对。主线新采样和全部正式比较仍在运行，尚无联合试跑结论。

先运行最多两轮、6000场上限。16:10不再开新轮，16:25切最终比较，16:50硬截止，为17:30前的假期部署保留时间。脚本脱离SSH/笔记本运行，冻结源码不可热改；最终是否进入假期长任务取决于实际结果和运行状态。

新源逐局归档已结束的日志，已核对一个仍在进行的批次中，单局完成标记、zstd文件、解压SHA一致，原文不再堆积到整批结束。空间预测同时计入活跃原文、全部待生成压缩包和50GiB余量。另有低优先级4线程任务，仅对两份已完成历史作业做无损gzip→zstd，不触碰当前训练输入。详细规则见[存储记录](experiment-storage.md)。

本机M4 Pro、Node26.5对四份真实输入（1–70实体、1–40受控对象、205区域）每份预热10次、计时60次，纯策略前向P95为6.8–10.7ms，释放后tensor数量无增加。这不包含观察编码、战术或图形渲染，尚不是完整客户端帧率验收。私有表`work/commander-coordination-20260924/inference-benchmark.json`。

相关代码检查：200项Node、类型检查；当前存储7项、序列7项、调度8项；四份真实episode/865决策跨Python、Node与恢复一致。Pro第3次答复已收取并核对，建议分开主线条件纠错与压力更新幅度；本轮根据诊断暂不增加成员辅助损失，也未把一个KL阈值当作唯一晋升条件。
