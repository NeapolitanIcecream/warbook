# 持续修复 0.1.9 问题及次生问题

2026-09-17 15:21 开始。用户要求持续分析与推进，直到这批问题及其次生问题修完。以具体问题闭环收尾，不以提交数、候选发布或某个胜率作为终点；沿用固定引擎、合法观察、Git 冻结和维护入口，不重做泛化预研。

## 待闭环项与证据

| 项目 | 当前证据 / 缺口 | 完成时要看到 |
| --- | --- | --- |
| 防守与 GI 火力 | 0.1.10 修复直接换点打断，部分局仍九名步兵损失，存在前出逐个接敌、护矿点漂移等疑点 | 两路可分配或先完成一路，必要支援仍发生；具体无谓部署/移动中断有解释或消失；新损耗须追到实际战斗原因 |
| 侦察与预警 | 只有主力找目标，没有独立侦察；空窗和迁移基地未及时发现 | 独立军犬带回有用敌情，路线/敌基地再观察，遇险处理；视野丢失不冒充敌人死亡 |
| 机会反击与经济协调 | 六车门槛与四车扩张冲突；四车机会只看装甲压力变小 | 打退攻势、暴露基地车等事件能触发有依据的成组出击；未出击有实际风险或侦察动作；预算不与当前作战目标冲突 |
| 坦克实际协同 | 同任务分走两桥，14:25/15:30 孤立损失；后续候选仍有孤立接敌 | 实际集合、支援与行军成立，落后成员不独自追击明显优势敌军；避免为集合造成新的长期停滞 |
| 矿场位置疑点 | 距矿 20–28 格；超时空矿车回程不能按步行计算 | 核对合法替代位置、实际出去采矿的路径与周期；证实有问题则修，不把未证实猜测一直留作“完成” |
| 分析本身 | 已加入取消部署与损失提示，仍缺信息/机会链 | 简短摘要能定位上述失败并引向具体片段；computer use + 最少源码/日志核实，识别正常转移，避免只看胜局或峰值 |

完成指已有反例、针对性决策检查与后续普通完整局支持这些行为修复，且没有留下由修复引出的同类缺口；不等于保证任意地图、任意对手都不犯错或要求全胜。不同版本、机制检查与完整局证据分别保存。

## 当前推进

- 从冻结 0.1.10 启动 `runs/closure-start`，短图起点 0，tick 15156 正常胜 Supalosa，真实运行可用。
- 第一项是独立侦察及其生产/单位归属，随后利用取得的敌情修正反击窗口；GI 火力和护援、坦克协同分别做小干预，不同时调整胜负规则。
- 每轮先固定版本和有限普通新局预算，读正负结果再决定下一轮。出现无效局部微调时改查机制；三轮实质迭代后检查结构与性能。候选达到整批闭环后更新默认玩家版本，并验证正常结束/退出/重开。

## 侦察与射程迭代

- 独立侦察 `ff19dd8` 对 0.1.10 的 16 次检查全部正常：侦察版 8/8、旧版 6/8；短图首次看到敌建筑可到 tick 3300，旧版有 7500–12750 的记录。出生位不同，不直接宣称胜率因果。
- 首次统计把 `own_objects_appeared` 当出生次数是错误的：同一条狗会短暂无法查询后重新出现。示例里七次 appeared 只有四条唯一引用、四次生产请求。`23c0973` 对短暂不可用做 150 tick 补产缓冲，并沿已走过的安全位置撤离；八场正常胜局，检查实际请求而非 appeared。不同出生位的首次探明时间仍有差异，四人图需继续改善覆盖。
- GI 次生问题由原回放取点确认：部分未部署士兵整格距离 5.1，实际距离约 4.5，已在 Para 范围；切换状态后的通用 30 tick 命令间隔还会延后攻击。固定 SDK `LEPTONS_PER_TILE=256`，Para `cellRangefinding=false`，地面射程使用世界坐标三维距离。`cf1b6ef` 增加己方/可见敌方位置，以及仅己方攻击状态；不透传敌方目标或冷却。修正射程判断和部署后攻击衔接。
- 射程筛查 16 次全正常，新版 8/8、侦察前版 6/8，但新版短图全出生 0。另固定八次覆盖检查，全部正常胜出：出生 1 对旧进攻一次、对 Supalosa 两次，各有四名 GI 损失；保留对旧进攻七辆坦克损失，坦克协同与反击选择未闭环。
- 完成三轮后做有针对性的结构整理：`c2e5e40` 把观察到的威胁、受损建筑与防守位置从反击/生产决策中分离，下一步在此基础上调整机会攻击；指令一致性检查单独执行。没有增加通用规划框架。

## 机会、分析和集群迭代

- 防守提取两局均正常，共 9,422 次同观察指令一致。`5a47c8c` 接入基于敌情的目标选择、实际集合与第一支六车预算；16 局全正常，7/8 对前版 8/8。首次六车在若干局提前到 tick 6756；仍有一次短图败局和估时过长导致的等待。
- `be6afa5` 分开防守位置与面向目标的集结位置，复用已探索路径，并根据己方实际位移估计速度；16 局全正常，8/8 对 7/8。仍不把这些不同出生位的小批总分解释为独立收益。
- `1193ae5` 修正真实武器能力：SDK 的 VirtualScanner 为 `neverUse=true`，不能把狗误当五格射程的反车辆/建筑武器。按静态装甲伤害能力分类，仍保留工程师/间谍对建筑的威胁。
- 分析在旧 0.1.9 回放准确指出 7:30–13:10 持有四到五辆坦克却无独立侦察的等待；新局记录精确的合法基地接触事件，避免十秒采样漏掉短暂可见目标。等待提示保留决策理由，不自动把所有等待判错。
- 新回放定位控件直接使用官方 ReplayScreen 的播放/暂停机制。固定脚本 URL 导致浏览器仍用旧包；加入包哈希参数后已验证。Computer use 成功前进到 7:35、8:02，再重放回 6:10，并自动暂停。`closure-operation-screen/mp06t2.map/supalosa/0-opportunities` 中四车围攻迁移基地，tick 7209 炮击摧毁展开后的 GACNST；画面和记录一致，整局 10:54 获胜。没有改回放动作或宣称 snapshot takeover。
- `cf29b92` 增加实际作战核心、共同装甲目标、落单退回支援、有限集结等待和真实地面汇合点。24 局全正常，两版各 10/12；短图对旧阵地改善，小镇两次失败。检查显示主要问题之一是未看见工厂时估计补兵为零，以及“近处只有一个坦克”的反击条件遗漏稍远的同队敌军；不能把这两次失败草率归为反复撤退。
- 矿场疑点已进入原局只读空间查询：第二矿场放置前存在 `(65,29)` 等合法已探索站点，比原 `(74,34)` 更接近可见矿。仍需核对实际出矿路径和新局效果，未标闭环。

接着修正未知产能/来援判断，补四人图侦察覆盖、按需回防，再完成矿场和集群复查。玩家入口仍保持已验证的 0.1.9 / 0.1.10，尚未发布中间版本。临时回放页 26/27 与 8643 服务已关闭。

## 2026-09-17 收尾检查点

用户要求在 17:30 前收尾，本轮停止新增功能与对局。实现证据冻结于 `fcad322`，工作分支 `codex/close-019`，尚未合并或发布。本轮共 107 次真实引擎执行均正常完成，包含不同候选、覆盖检查及两次重构一致性检查，不合并计算胜率；最近的集群比较 24 局，两版各 10/12，地图/对手之间有胜负翻转，不能认定全部问题已修复。

下一轮先修未侦察到的敌方产能被当成零、近区反击漏算稍远同队敌军；随后处理按需回防、四起点地图的侦察覆盖、矿场实际出矿路径与经济验证，并复查集群及整体验收。矿场选址草稿未接线、未测试，已移出活动源码，保存在忽略目录 `work/closure-019/refinery-site-draft.ts`；不要把它当成已完成实现。

默认玩家入口仍为 0.1.9，挑战入口为 0.1.10；本轮代码尚未切入玩家入口。临时回放页和 8643 候选服务已关闭，保留维护入口 8642/PID 89005。完整局、回放与本地探针留在忽略目录，Git 只保存源码、实验计划及证据索引。

## 2026-09-18 09:15 恢复推进

用户已授权继续，沿用此范围。冻结昨日候选重新完成小镇普通局 `runs/closure-resume-0918`：出生 2，tick 25212 正常败北，5.93 秒，未重设种子。

- `927ec7a` 统一目标及局部反击的敌军支援估计；保留迷雾下已见工厂，未见工厂采用明确标注的一处可能产能先验。原败局稀疏观察检查在 tick 6600 从零补兵改为两辆可能到援；15000 近区反击由一辆敌车改为 5.75 装甲当量，并拒绝原危险出击。它是局部决策检查，不冒充整局精确反事实。证据 `work/closure-019/support-recorded-check.json`。
- `closure-support-screen` 16 局均正常，新版 5/8、前版 6/8，起点不匹配；对 Supalosa 均 4/4，小镇对旧阵地均 1/2，短图新版 0/2 对前版 1/2。并未证明更强。短图反例在 6:55 四车出击，8:00 已有五个近处硬目标，战略仍保留出发估计。Computer use 查看 7:50 / 8:02：四车已成组到桥口，守方坦克从矿区开火，后续两车损失；因此下一改动针对出发后不再更新估计，不能再归为未出击或孤车。临时页及 8643 服务已关闭。
- 本轮第二个候选重新评估行军与接敌中的作战任务，只有近距离即将摧毁的目标保留收尾承诺；真实对照正在 `closure-reassessment-screen` 执行。

- `1bb8d40` 行军中重估：16 局均正常，5/8 对前版 6/8，仍未显示整体优势。短图反例八分钟出击取消后有两分钟集结等待，没有长期进退死循环；保留具体负局，继续查防守/经济限制。
- `05f5d05` 四起点两狗独立路线及已探索起点共享；12 局均正常，4/6 对单狗 5/6，起点不同，不能宣称胜率收益。新局实有两只狗及三至四个起点被探索，但部分敌方工厂仍较晚看到。旧版本无 exploredStarts 字段，不能把旧版缺字段当作“零覆盖”。新增替补测试的只读类型错误已在 `0201d3f` 修正；冻结行为代码不变。
- `8e23062` 按需回防：16 局均正常，两版各 6/8。短图真实采样有四车继续进攻、一车救援，小镇有六车继续、一车救援；仍保留小镇两次失败，不把分配人数当作已证明防守有效。将回防分配从进攻阶段中拆出，保持已有单位归属检查。三轮结构复查未发现需要泛化重写或吞吐优化的依据。
- 原 0.1.9 展示回放通过完整终局复核，提取 24 次己方卸货至再次采矿的真实行程：第二矿场后代表片段约 22–28 格 / 33–43 秒，包含行驶、转向及等待，排除满载超时空回程。`work/closure-019/mining-cycles.mjs` 和原局 `mining-cycles.json` 保留原始证据。只读查询在 tick 10974 选出合法 `(64,31)`，通往可见矿的预计出矿路线 9.49 格；不能当作已发生的节省。
- `51bdc06` 矿场筛查 16 局：15 次正常结束、1 次运行上限；候选五胜二负一上限，前版五胜三负。短图同出生 1 的新局，首矿位置与最初三次出矿路径相同；新第二矿 `(65,29)` 的 19 次出矿行程中位数 13.25 格 / 25.6 秒，原首矿 14 次为 34.33 格 / 52.33 秒。两组处于不同采矿阶段，不能直接当作净收入增益；它确认合法新位置实际缩短了出去采矿的路。四份采矿轨迹都通过原停止状态核对。
- 上限局 `closure-refinery-screen/mp06t2.map/old-defense/0-ore-routes` 已通过 720 份快照与原停止状态复核：有进攻和建筑战果，不能称从未攻击。tick 51000 三车估计能承担当前风险，却被非暴露目标必须四车的硬门槛拦住。`d2a1a1e` 删除该门槛，保留实际成组、风险门槛及未知目标六车探索；同份稀疏合法观察现在批准三车攻击工厂，见 `work/closure-019/strength-recorded-check.json`。
- `closure-strength-screen` 16 局均正常，候选 7/8 对前版 6/8，起点不同，保留短图对旧阵地一负。四图综合 `closure-integrated-check` 的 48 局全部正常：候选 18/24、已发布 0.1.10 为 20/24；对旧主线均 7/8，对 Supalosa 为 8/8 对 7/8，对旧阵地 3/8 对 6/8。候选尚不晋升。
- 成功局仍有反例：`closure-strength-screen/mp06t2.map/supalosa/1-strength-gated` 在 6:20 损失的坦克，最近队友已在 10.4 格外。Computer use 的 6:14 画面确认残血车留在桥上而队友退远；最少日志核查显示 5532 撤离命令后，5562 和 5622 被普通防守的碾压命令覆盖。`557dfc2` 为取消进攻添加独立撤退阶段，以原生 Move 退回支援位置，抵达后才恢复防守/集结，不靠计时自动结束。保留普通接战的碾压能力；16 局冻结对照 `closure-withdrawal-screen` 正在执行。临时回放和 8643 服务已关闭。
- 撤退对照实际为 15 正常、1 上限；新版本 6 胜 1 负 1 上限，前版 6 胜 2 负。上限局四车从 tick 20415 起一直无法返回 `(41,68)`。只读原生寻路在 22050 确认：该格不可通行且路径为空，已有通路点 `(34,68)` 可通行，四辆受阻车均能找到 26–32 节点路径。证据 `work/closure-019/withdrawal-path.json`；不是用延长超时掩盖卡死。
- `53ef7bd` 修正 DefenseSituation：敌军位置变化不再使已有合法通路失效并退回未检查的直线点，初始没有通路时使用已知基地起点。其对照 16 次中旧版有一次上限，新版八局全部正常（6/8 胜），最长撤退 85.2 秒，撤退期间攻击/碾压/攻击移动指令均为零；短图两个出生方向都完成胜局。89 项测试与类型检查通过。
- 小镇 `closure-ground-post-screen/mp03t4.map/old-defense/0-ground-post` 的 46:21 败局复核 558 份快照一致。早期六车和四矿车目标均已正常触发，排除本局经济目标永久停留两矿车的解释。首次 12 车攻击时，对方 13 辆坦克已有合法可见位置，但目标估计只有 7.6 装甲当量：判定只检查敌人能否及时返回目标基地，遗漏能向我军截击的野战部队。`a9b91b7` 同时计入可到达我方接触区的力量，保持仅合法观察；新一轮 16 局对照 `closure-interception-screen` 正在运行。
