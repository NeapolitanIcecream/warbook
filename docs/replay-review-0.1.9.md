# 0.1.9 展示回放：玩家反馈前的独立复查

2026-09-17。按用户要求，在收到这次具体玩家观察前先固定判断。此回放已经用于开发和发布展示，**不是未见测试集**；“独立”仅指未根据本次玩家反馈调整结论。本次没有修改策略、默认分析工具或试玩版本。

对象：`runs/active-defense-release-check/mp06t2.map/supalosa/0-release`，WarbookRed 对 SupalosaBlue，17:12 正常胜利。HTTP 回放与原文件 SHA 一致：`d58f39f549cedcddce0203dba28959ecb2ec2357006a30b130c45559fbf1c855`。

使用现成摘要、若干关键画面和必要日志，再对原回放做只读取点。画面包括 8:08/10:25 的己方等待、10:25/12:34 的敌方阵地、13:37 分桥行军、14:37 交战及 17:12 终局；不宣称逐秒肉眼看完。早期姿态切换由重放状态核对。补查在 `work/independent-0.1.9-review/inspect.mjs`，结果 `evidence.json`；终局 tick、资金与败北状态再次匹配。

## A1：六车任务没有形成相互支援的战斗群（高信度）

- 13:19 发出进攻任务时，五辆坦克已经在基地西侧，最后一辆仍在 `(76,40)`，距最近的其他主力约 10.4 格。就绪条件允许它离预备点 12 格，并没有确认实际集合。
- 13:37 的画面显示坦克分走两座桥，队列拉开。重放中的五辆位于 `(48–54,34–45)`，另一辆在 `(67,56)`。
- 14:25（tick 12978），后一辆在 `(47,78)` 被敌方坦克击毁；最近友军约 13.6 格外，附近有三辆敌方坦克。
- 15:30 左右（tick 13948），另一个主力成员在 `(45,74)` 被击毁，最近友军约七格外，附近有四辆敌方坦克。

这两次就是本局己方坦克损失。分路本身可以有价值，但这里没有对应的小队协同，出现了超出友军射程的单车接战。不能把“六辆分配到同一个任务”解释成六辆一起打团。

## A2：大兵仍有无效部署切换与不稳定换防（高信度）

- 5:39 附近，两名 GI 在 tick 5088 进入部署状态，分别在 5091、5094 解除；约 0.2 秒、0.4 秒，期间均无武器冷却重启信号。另有 5:02–5:06 左右约 3.8 秒的部署后取消、无开火片段。
- 5:34–6:20 的全局守备目的地在 `(74,45)`、`(63,35)` 等相距较远的位置间多次变化。命令和位置记录显示，部队确实发生重新移动，而不只是计划标签变化。
- 两名己方 GI 在约 5:02、5:04 被击毁，随后补齐。期末六名步兵不等于首批六名都存活。

事实是姿态反复且存在无火力片段。全局防守点重定位、责任区过滤与局部部署条件之间的交互是目前最值得检查的原因；尚未通过干预分离各自影响，不能直接宣布某个阈值就是唯一根因。

## A3：经济投入、成军条件与信息获取没有协调（事实高信度，最佳替代需验证）

- 第四辆坦克在 7:26 出现，同时启动第二座矿场和额外矿车；第五辆直到 12:20 才出现，间隔约 4 分 54 秒。
- 经济规则以四辆坦克触发扩张，反击仍等六辆。两条生产线占用资金时，战斗单位补充出现长空档，直到 13:19 才出发。
- 约 6:30–13:50 的稀疏记录中没有可见敌军接触，期间没有主动侦察/施压任务。13:52 的进攻计划才重新记录接触。
- 四车反击窗口专门检查装甲压力下降；本局早期来袭以步兵为主，因此这个窗口没有解决打退步兵攻势后的决策。

回放真值显示对手仍在发展，但 actor 当时没有完整敌情。不能据此要求它全知地选择某一秒进攻；问题在于它长时间既不探明情况，也不重新协调等待目标与花钱计划。

## A4：矿场选址可能拖累经济（中等信度，待验证）

首座矿场位于 `(71,40)`，实际采矿点一度在 `(60,23)`、`(57,16)`，直线距离约 20–28 格；第二座矿场 `(74,34)` 仍偏向既有基地，未明显贴近后来的采矿区域。选址代码主要按基地位置排序，没有以出矿路径为目标。

这是布局与去矿路径的疑点，不能直接把直线距离换算为往返耗时、损失收入或最佳建造点；尤其不能忽略超时空矿车的回程方式。需要另行核对可建位置与实际采矿周期，才决定如何改。

## 这次暴露的分析方法问题

1. **用任务和数量代替实际协同。** 当前摘要报告六车出发和首次离开基地，却没有先列本局两次坦克损失及当时队友位置。
2. **用峰值和一个窗口代替稳定火力。** 4:30–4:59 的“前两秒内最多五名步兵开过火”只在一个三 tick 采样点达到峰值。约 73% 采样时刻前两秒没有步兵开火信号；其中包含接敌/转场，不能全算浪费，但显然不能用峰值代表持续输出。该窗口还恰好停在两次步兵损失之前。
3. **只在 advance 阶段找停滞。** 以 defend 名义发生的长时间等待不会触发行军停滞检查；经济、侦察和军事目标没有被放到同一条时间线上解释。
4. **我对验收数字的解释过度。** 有部署、有开火、有正常胜局，证明功能开始起作用，不能据此宣布步兵防守和集群质量已经解决。需要优先看损失、动作反复和实际相互支援，不是继续堆更多输出。

碾压仍是实际强项。早期敌方步兵的击毁归属包括 GI、工事和坦克，后半还有精英坦克的有效杀伤；击毁归属不等于边际贡献。胜局与这些强项应保留，但与上述控制缺陷分开判断。

以上 A1–A4 及方法判断作为本轮用户反馈前的固定版本，之后对照遗漏、误判和解释偏差；不事后改成与玩家观察一致的“原始判断”。本次临时回放页已关闭，维护入口保持 0.1.9。
