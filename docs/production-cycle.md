# 生产记账与补兵：2026-09-16

12:30 按用户“继续”开始。第一轮只改生产控制器，保持 0.1.6 战略、战术、观察、执行和裁判协议。玩家入口继续固定 0.1.6，候选验证后单独决定发布。

## 问题与已核对的事实

- 旧败局 `runs/assembly-recheck/mp03t4.map/m0/0-assembly`：第二矿场与矿车同时投入，出现第五辆矿车以及首批四辆坦克后的增援空档。
- 本轮用原始回放逐 tick 重建，310 份原观察快照及终局全部吻合。第二矿场在 5887 进入生产，7354 已有三辆矿车，7696 又开始生产矿车，8580 矿场完成并送出第四辆，9148 车辆队列送出第五辆；下一辆坦克至 9760 才出现。事实支持漏算矿场附带单位，不单独证明其导致整局败北。
- 固定 SDK 源码 `FreeUnitTrait` 在建筑由 BuildUp(0) 进入 Ready(1) 时生成附带单位。该回放实际规则为 `GAREFN → CMIN`、`NAREFN → HARV`，这两种矿场各一辆；无法找到出生位置时返还单位价值。因此承诺只计到矿场 Ready，之后按实际矿车补齐，不能永久认定已经送出。
- 当前 0.1.6 首局 `runs/production-start-check` 正常结束，tick 16947，败于冻结 counter。此局只达到四辆矿车：战略用当前坦克数决定经济目标，坦克损失也会让目标下降。因此不能把第五辆矿车描述为每局必现。

## 最小修正

生产控制器根据已有合法观察，计入存活矿车、车辆队列中的矿车、建筑队列中的矿场、已放置但仍 BuildUp 的矿场，以及本次已通过队列/资金检查的新矿场请求。已就绪待放置矿场只计一次；仅有未来建造目标不占名额。

从当前观察重新计算，避免取消、摧毁或生成失败留下永久承诺；不取消已有队列，不改变战略扩矿目标、兵种优先级或资金门槛。两类附带单位使用固定 0.83.3 标准规则的名称，未扩展可变规则平台。

机制检查覆盖盟军/苏军的队列→等待放置→施工→实际矿车生命周期、取消/摧毁、无法生成、计划尚未入队和已有矿车队列。运行中的事实通过现有 control_report 记录，不增加策略可见信息。

## 本轮比较与保留条件

1. 固定 0.1.6 `e3e338f` 与候选，先在已用的美国小镇和短图对冻结 0.1.6、counter 做每格 4 次的筛选，共 32 次。查看实际矿车承诺、第四辆坦克后下一辆坦克的间隔、完整胜负及异常；普通新局不假定精确配对。
2. 行为确实修正且没有明显异常时，固定四张已用地图、0.1.6/counter/Supalosa，每格 6 次，两版本共 144 次。结果分别报告地图与对手；这是开发回归，不冒充未见来源或正式综合增益确认。
3. 若只是少一辆矿车却没有改善补兵空档，另开战略预算对照；若出现具体退化，诊断反例后决定是否保留。记账正确性与棋力增益分开判断。

本轮范围先完成这项候选、完整局对照与发布决定。持续行军协同及收入枯竭恢复仍是独立后续问题，不夹带进本次改动。

## 首轮实际结果

候选来源 `e4e7c99add15179096f3be33c23003cb400b48d6`，Node SHA-256 `8e255c1b8700a6c17572cb45ee6095b6be23496f0a6262e9a57b94e15dbdf3c0`。与 0.1.6 冻结包逐个打包输入 hash 比较，仅 `src/control/production.ts` 不同。40 项测试和类型检查通过。

[production-screen.json](../experiments/production-screen.json) 的 32 次全部正常结束：旧版 7/16，候选 8/16。前 12000 tick 稀疏观察中，旧版 4 局出现超过四辆矿车，候选 0 局。小镇各 8 局，第四至第五辆坦克首次出现的间隔中位数为旧版 2967 tick、候选 2406 tick；短图两版各 3 局在终局前未出现第五辆坦克，这些缺失单列，不按零间隔计入。起点未控且不完全均衡，间隔差不是精确配对效应。

候选代表局 `production-screen/mp03t4.map/published/1-counted` 的 replay、182 份原观察及终局核对通过：第三辆矿车在 7041 出现；7315 车辆队列开始坦克，此时第二矿场仍在生产；8253 矿场完成送出第四辆矿车，8261 第五辆坦克出现。说明预期生产选择真实发生，不把不同起点的两段回放当作同局反事实。

现有 `analyze` 继续负责胜负和版本核对。新增窄用途命令 `python3 analysis/production_events.py runs/production-screen` 从已有日志输出逐局出兵时间和矿车观察；其指标定义为累计首次出现与稀疏矿车数量，不宣称精确建造完成帧、同时存活兵力或胜因。它解决本次生产间隔无法从总胜率看出的具体问题。

筛选支持执行预定的 [production-pool.json](../experiments/production-pool.json) 144 次比较；没有根据当前胜场修改地图、对手或预算。

## 四图完整局比较

`runs/production-pool` 的 144 次全部正常结束。固定池结果：

| 地图 | 0.1.6 | 记账候选 |
| --- | ---: | ---: |
| mp03t4 小镇 | 11/18 | 15/18 |
| mp06t2 短图 | 12/18 | 13/18 |
| mp29u2 城市 | 17/18 | 14/18 |
| mp08t2 | 12/18 | 11/18 |
| 合计 | 52/72 | 53/72 |

候选直接对冻结 0.1.6 为 15/24；0.1.6 镜像被评估方也是 15/24，镜像单列，不能作为候选必胜或初始化无偏的证据。对 counter 为 18/24 对旧版 15/24；对 Supalosa 为 20/24 对旧版 22/24。各版本/地图起点分布不同，全部创建槽位均交替。总体仅一场差距，不声称综合强度提升。

前 12000 tick 观察到超过四辆矿车的对局从旧版 18/72 变成候选 3/72。候选剩余三局全部为 `mp08t2/published/{1,3,4}-counted`：tick 7668 矿场被毁，原四辆矿车仍在，战略后来重建第二矿场，9906 又送出一辆。车辆工厂期间持续造坦克，没有重复购买矿车。第 1 局 replay 的 138 份原快照及终局核对通过。这是两座矿场目标带来的附带单位，不能把“超过四辆”直接等同于记账错误。

补兵间隔有条件差异：小镇中位数 2992.5→2481 tick，但短图为 2058→3345，城市为 2295→2571。短图旧版 5 局、候选 3 局尚未出现第五辆坦克便结束；mp08t2 则分别 7/8 局缺失。这些是不同真实轨迹上的描述统计，既有选择差异，也有战斗和资金反馈；修正没有普遍消除补兵空档。

## 城市切片复查与保留决定

城市对 Supalosa 的首批结果为旧版 6/6、候选 4/6。候选两场失败来自出生位 0，旧版该起点只有两场。候选 `mp29u2/supalosa/1-counted` 已完成密集回放核对：tick 9000 首批坦克已全失，第三辆矿车在 8037 出现，第五辆坦克要等至 9423。它暴露了原有开局扩张和增援时序问题，但不同普通新局不能单独确定该败局由少一辆矿车造成。

在结果产生前固定 [production-urban-check.json](../experiments/production-urban-check.json) 的 24 次复查，不更换双方代码：

- 候选 11/12 胜，全部正常结束；出生位 0 为 5/6，出生位 1 为 6/6。
- 旧版 9/12 胜、2 负、1 次 tick 上限；出生位 0 为 3/5，出生位 1 为 6/7（含上限）。
- 上限局 `mp29u2/supalosa/1-published` 停在 54000、引擎 Started、无 turn-manager 错误，双方未败北。最后观察仍有五辆矿车和大量坦克，不能套用上一轮“无矿车、225 资金”的解释；原因未归定，保留为未决，不计平局或胜局。

早先城市差距未在补查中稳定重复；补查单独报告，不并入四图得分。决定保留修正作为生产正确性改进，继续保留 0.1.6 冻结版本，不宣称综合棋力增强。下一项战略实验应单独比较扩矿与连续补兵的时机；持续行军协同和收入恢复仍另行处理。

## 0.1.7 发布包检查

最终源码 `8326311288ec685c3da43b1d182071dc36938fe8` 仅在已评估候选上修改版本元数据。Node SHA-256 为 `377302a5931b6ecdeae39b55809c6eabe1e3781b48c60c1359b3da3db3efaa03`，图形包为 `632848aabb1c7e73ee9db7946ab980d03747ea55080c90ea576de3d88605a72c`。`production-release-check` 四局正常结束，16,838 次同观察指令比较与已评估候选零差异；不要求新策略继续匹配旧生产行为。

`runs/production-demo` 是最终 0.1.7 对冻结 0.1.6 的单局观战样本，新版 WarbookRed 在 22647 tick 败北。新版保持四辆矿车，下一辆坦克在 8397 被观察到；旧版出现第五辆矿车，下一辆坦克在 9432 被观察到。旧版最终仍获胜，再次说明提前补兵不等于整局必胜。原始回放重放至相同 tick，资金 1175/5398 及败北状态一致，SHA-256 `6b674d240e15ba8093ac4aeeb49767bcea86d9edd24dd62394fcf5d308a60bca`。

本轮共有 206 次新的 headless 启动，205 次正常结束、1 次旧版上限，跨越不同用途，不能合并成一个版本的胜率。

最终玩家包完成首页→本地对战→鼠标/键盘展开→正常结算 9:53→重开→生产/放置电厂→主动退出 5:53。第一局 Game.onEnd 为 tick 8755、退出为 8906；第二局主动退出 tick 5301、引擎仍 Started，不能记为正常胜负。客户端产生 30 条战略计划、95 条控制器报告，实际使用 `queue-production-v2`；证据在 `runs/player/production-browser-check.json`。这属于操作链路检查，不是竞技真人成绩。

12:54 正式 8642 服务已切到 0.1.7，PID 26756。主包、counter 包及新观战 replay 的 HTTP 哈希全部核对，结果在 `runs/player/production-release-check.json`。候选服务、完成的 Node 实例和玩家测试页已关闭；资源、日志、回放及本地配置继续留在忽略目录。
