# 长期信用分配的小对照

2026-09-22。局部机动实验正在按冻结源`3c9386d`执行；本项准备另一种机制，不覆盖其训练器或改变其中的优势计算。

## 具体问题与依据

信息对照中新增事实会改变value，但较少改变所查接敌状态的argmax动作；新局部动作已经被真实尝试，但终局MC信号仍会把整场后果带给很多不同操作。它不表示MC传播不了长期奖励，也不证明某次局部止损本来能赢。本项只检验旧value的时间差能否降低这种噪声。

用户授权的[Pro咨询第四轮](https://chatgpt.com/c/6ab1e596-5fc4-83ec-bda3-c2013c713800)读取了固定`3c9386d`，建议仅比较λ=1与.99。已核对[GAE原论文第5节](https://arxiv.org/html/1506.02438v6#S5)：GAE可以配合MC价值回归，优势使用更新前的value；减小λ在降低方差的同时会引入预测误差带来的偏差。这里不迁移训练框架，也不增加击杀/走位奖励。

在最近完成的一块on-policy主线local47采样中，每个地图/对手取repeat0，共24局。按局平均的value平方误差为0.183，可行动状态为0.179，同样24局的常数胜率预测为0.243；记录间隔均75 tick、最大末端间隔72 tick。这仅是开发数据上的实用检查，不是未见校准、优势准确性或GAE收益证明。

## 保持什么、只改什么

- 胜负效用继续W=1、L/U=0，E排除；γ=1，末端bootstrap为0。上限属于当前有限时域目标的未胜终局，不套用通用的超时bootstrap。
- MC臂保留原`ret-oldvalue`路径；GAE臂在每局内按时间倒序递推δ，再展平。中间即时奖励为0，只在最后转移给一次终局结果。强制KEEP参与递推和critic监督，仅在actor损失及优势归一化时排除。
- critic仍对广播的MC终局结果做回归，使用当前原有sigmoid/MSE/联合梯度裁剪；不把`A+V`换成critic目标。一批内优势固定，不使用本批更新后的value重新计算。
- `method`保持`ppo`，继承Adam；记录γ、λ和critic目标。旧模型缺省仍λ=1，已标记λ的模型续训时沿用，显式参数可覆盖。
- 只比较1和.99，不扫.95。75 tick约5游戏秒，.99对90秒后的残差仍保留约0.83权重；不把这个工作默认值说成已证最优。

回归覆盖λ=1逐行等价、W/L/U、多episode边界、强制KEEP、即时奖励仅一次，以及中间漏行/末端缺口拒绝。真实完整批次的数值对照、训练导出和完整局结果仍待执行。

`0db94a6`已在服务器用原同一192局、checkpoint、Adam、seed73047重训：λ=1的actor和critic权重与原MC更新逐项完全相同。相同数据的λ=.99也已完成训练和跨运行时概率检查。这只验证实现和兼容性，尚无GAE强度结论。

## 准备的实验

局部菜单结果收取后固定一种菜单。每路线从同一checkpoint和Adam分叉MC/.99，各47/83两重复；首轮可共享同一checkpoint的on-policy采样，之后各自重新采样。主线/压力继续交替、轮内同池，保留未更新起点并用完整局比较。没有受控出生或快照分叉。

先跑有界比较，不把优势符号更合直觉、loss下降或value更乐观当作成功。特别检查是否更频繁回家/超时；value可能把脱离敌人视野错误判断为变好。若结果很小或退化，保留负结果并降低这个配方的优先级。

运行器已支持共享每路线/种子的首批采样、独立后续采样与固定迭代检查点。首批共享前核对两个行为checkpoint的SHA；训练边界保留所有已导出且校验的pending更新参与最终比较，不自动把它们当冠军。命令超时会停止本阶段进程组并转收尾；真正错误仍保存失败状态。新增边界/检查点回归通过。更长的离线预算及起点将在当前菜单结果收取后确定，不把准备完成说成训练已启动。

### 离线预算工作默认值（待菜单结果决定起点后启动）

计划同一固定菜单下MC/.99、两路线、47/83两个重复，最多24个联合轮次；每模型最多4,608局使用量。第一轮每种子两臂共享，因此实际最多36,096场新RL对局。完整保存第3、第12次和最新更新，最终在同一冻结池一起比较；每路线最多15份候选各224局，最终最多6,720局，合计42,816局。较早模型不被最新模型覆盖，也不依赖逐轮小样本排名选冠军。

这个预算把方法对照扩到接近之前每模型约5,000局的量级，同时保留较短预算的端点，区分方法与训练量；不是把只跑576局的结果称为充分训练。拟定2026-09-23 06:45不再开始训练阶段，09:00硬截止收取，使用已测192路CPU并发、批次压缩和空间余量。若时间边界提前到达，按实际更新/游戏数报告，保留已验证pending，不把计划数量写成完成数量。此处是工作默认值，尚无正式启动收据。

## 15:52正式启动

`f6d6d6e`的24局管线预检全部正常（20胜4负、0E）；四份训练使用4场独立采样、各方法合计8次整局使用，逐份核实配对数据列表/输入SHA、λ和MC critic目标。完成目录的独立副本触发STOP_TRAINING后正常转最终比较，current权重未重置，没有新开对局；pending及中间检查点另有回归覆盖。

2026-09-22 15:52已在loreley-115独立tmux启动上述24轮预算，冻结源`f6d6d6e`。主线取已完成局部实验local83端点`352319e4…`，压力取local47端点`d52dbe17…`；固定local菜单继续研究信用分配，不声称它们已胜过所有保留参考。所有旧强模型保留。两方法第一轮共享数据、后续各自重新采样，06:45训练边界、09:00硬截止。

SSH退出后再次连接已看到384局首块完成、主线四份更新/概率检查完成并进入压力采样。两随机重复的MC/.99首批列表及输入SHA相同，λ/critic目标符合计划。任务与恢复位置在忽略文件`work/server-feasibility/active-job.json`的`creditNight`，当前不依赖笔记本。已存约343 GiB可用空间；批次压缩、空间检查和截止机制继续生效。明早按实际status/state/exit/audit收取，不把42,816计划局数当作已完成数量。
