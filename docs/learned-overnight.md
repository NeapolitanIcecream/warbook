# 2026-09-20 过夜学习实验

## 2026-09-21 收取结果：任务提前中止

**正式任务在9月20日18:31:50中止，未完成计划中的过夜联合演化。** 实际执行6,288局，已逐局核对原始result/manifest、角色、计划覆盖及模型SHA：5,995次正常胜负、292次预定tick上限、一次双方败北的引擎未决终局。原批次把最后一种归为E，导致整个编排退出；原记录和失败状态保留。这次未实现持续过夜运行。

已完成的主要工作是更大规模的起点比较、线性搜索，以及47号主线的一次512局PPO更新和下一批512局采样；后者未进入原定更新。压力路线新增了一个BC初始化，但没有完成新的PPO适应；完整主线/压力交替轮数为0。下方计划局数均不能作为完成量。

### 初始化匹配比较

每主体256局：四张开发地图，对主线0.1.16、压力0.1.16、旧阵地0.1.8、Supalosa各64局。全部正常胜负或预定上限，无执行错误。

| 主体 | 胜 | 负 | 上限 | 胜/总局数 |
| --- | ---: | ---: | ---: | ---: |
| PPO 47 | 190 | 57 | 9 | 74.2% |
| 其BC 47 | 169 | 81 | 6 | 66.0% |
| PPO 83 | 196 | 48 | 12 | 76.6% |
| 其BC 83 | 192 | 61 | 3 | 75.0% |
| 原BC | 173 | 78 | 5 | 67.6% |
| 线性 | 189 | 56 | 11 | 73.8% |
| 接口规则老师 | 170 | 78 | 8 | 66.4% |
| 稳定0.1.16 | 175 | 72 | 9 | 68.4% |

47的+21胜中有+16来自压力0.1.16（29/64→45/64），但`mp03t4`从41/64降至38/64。83仅比其BC多4胜，Supalosa从64/64降为61/64（1负、2上限）；其原有实力不能全部归于RL。线性与47仅差1胜。当前支持局部学习收益的信号，仍不支持跨初始化、所有地图或普遍优于简单方法的结论。

压力四主体各128局，对主线0.1.16、新冻结主线、旧阵地及压力0.1.16各32局：PPO61胜62负5上限，其BC52胜70负6上限，接口老师58胜61负9上限，原压力53胜70负5上限。对新主线PPO10/32、BC5/32，仍需更多证据；不与先前不同对手池的16/32直接相减。

64组新线性参数在初筛中最高15/16，但复查未超过原参数：原线性96/128，新入选者86、79、84、91/128。因此保留原线性。只保留最高小样本分数会夸大进展。

### 吞吐和中止原因

同一冻结策略、同分布各512局：96路398.0秒，约4,631局/小时、29,790 tick/秒、采样RSS峰值85.5GiB；128路341.6秒，约5,395局/小时、35,820 tick/秒、110.1GiB，整块平均约92.8核。后续选择128路。这是该工作负载的测量，不是持续运行保证。

中止局在49,084 tick结束，引擎为Ended、turn-manager无错误，双方均标记败北。固定SDK源码的僵局检测倒计时结束会令所有参与者败北；本地回放核对220/220原单位快照、终止tick、资金及败北标记均一致。该局不能记为单方胜负，也不能将终止帧的统一清理事件当普通交战战果。原回放SHA `64a3e2bb14f12890511abf3c5d14167e3ac2f203f0c4d0c631e1f288033f6f15`，本地在 `runs/overnight-unresolved-20260921`；原result另存为result.remote.json，仅本地复放副本调整路径。

修复 `c3f6784` 让采样和训练共用终局分类：无错误的双方败北为U，有限窗口未胜效用0；真实异常、墙钟截断及证据不一致仍为E。不是把未决当正常败局。13项分析测试通过；对6,288份原记录重新运行分类，仅上述E→U一项变化，全部原胜场不变。原始失败批次没有改写。

### 上午补救（单独记录）

已用昨晚采好的512局数据补完被中断的更新，来源与旧行为模型SHA核对通过，并完成真实多动作的PyTorch/TF.js导出核对；这属于9月21日上午工作。

首次复验暴露旧冻结包位于另一工作树时导入另一份SDK、Bot类型检查失败，已停止该批并保留诊断结果，未并入实力成绩。相同源码与权重在当前目录重建后，产物字节完全相同。`29d1333` 在启动批次前将验证过的冻结包原样放入当前运行环境，使依赖解析到同一SDK，保留原路径/哈希，不放宽SDK和驱动检查。两场跨工作树加载的真实对局检查通过。

随后独立完成1,280局复验：八主体，四张地图、四个固定对手加原冻结自适应压力对手，每主体160局。逐局复算1,227正常胜负、53预定上限、零错误；模型SHA、角色、全部计划格子一致，程序退出码0。恢复更新的多动作导出误差9.94e-8。这些属于上午工作，不混称为昨晚已完成。

| 主体 | 四个固定对手合计（128局） | 自适应压力（32局） | 总计（160局） | 负 / 上限 |
| --- | ---: | ---: | ---: | ---: |
| 昨晚追加更新0 | 108 | 12 | 120 | 37 / 3 |
| 上午补完更新1 | 92 | 15 | 107 | 46 / 7 |
| 原PPO47 | 100 | 13 | 113 | 41 / 6 |
| BC47 | 80 | 17 | 97 | 59 / 4 |
| 原PPO83 | 96 | 21 | 117 | 36 / 7 |
| BC83 | 104 | 15 | 119 | 35 / 6 |
| 线性 | 82 | 11 | 93 | 53 / 14 |
| 稳定0.1.16 | 82 | 19 | 101 | 53 / 6 |

**采用后一次更新没有带来稳定提高。** 更新0→1总胜场120→107；固定池108→92，自适应压力12→15，存在取舍，不能仅按训练loss或最后checkpoint晋升。更新0与BC83总分仅差1胜；PPO83对自适应压力21/32高于更新0的12/32。保留这些不同响应的候选继续交叉验证，不只保留平均分最高者。

原PPO/BC/线性的SHA跨两批一致；仅合并同一四对手固定池（不含新增的第五对手），每主体384局：PPO47为290胜83负11上限、其BC249胜125负10上限；PPO83为292胜74负18上限、其BC296胜81负7上限；线性271胜91负22上限、稳定基线257胜112负15上限。47起点的收益在两批中重复出现，83起点没有稳定PPO增益。编号包含初始化和BC整局划分差异，不把两起点差距单独归因于随机权重。

目前没有完整的新一轮主线/压力联合演化，也未晋升玩家版本。接下来优先补压力路线与这些主线候选的交叉适应；比较每次更新前后的checkpoint，避免跳过有价值的中间权重。新旧压力对手不同导致的分数差异要单列，不能泛称全面增强。

全部相关tmux会话已结束，没有遗留的本次游戏进程。精确状态、审计和汇总见 `work/server-feasibility/active-job.json` 的morning项、`harvest-audit.json` 和 `harvest-fixed-pool.json`；原始产物保存在服务器和忽略目录。

---

用户要求17:30前启动，开发机下线后继续使用CPU服务器，明早9:00查看结果。沿已实现的MLP新波次接口继续，玩家入口仍0.1.16；Qwen仅作[另项设计研究](nontext-pretrained-policy.md)。

## 要解决的问题

上一批主线重复47/83为50/64、53/64，缺少各自BC起点；压力PPO与其BC都16/32。因此先补匹配比较，再检查更多真实交互和交替对手适应是否产生可重复收益。完整旧结果见[学习实施](learned-launch-cycle.md#扩展已于1617完成尚未晋升)。

## 有界执行

- 沿用固定引擎、合法观察、33动作、现有PPO与正常胜/负/预定上限口径。没有新策略接口或奖励更改。
- 先运行小规模端到端预检，覆盖实际游戏、两个路线的更新、导出核对、冻结对手和最终比较。仅预检正常完成才自动开始正式任务。
- 同一模型分别96/128路、每档512局，按完整块引擎tick吞吐选择并发。旧128局小块的长尾不足以证明更大矩阵的最佳并发。
- 主线八主体各256局，包含两份PPO各自BC、原BC、线性、接口老师和稳定0.1.16；压力四主体各128局，补足起点证据。
- 增加64组线性参数筛选，再复查少量入选者与原线性，保留便宜路线的竞争。
- 最多八轮主线→压力交替。每条路线两条训练轨迹，每轮每条轨迹两次512局PPO；对手冻结后再开始另一方适应，保留历史对手与稳定基线。开发比较只决定后续对手，不自动发布。
- 每轮保留旧候选作同批比较；相同胜场优先保留旧者。连续两轮两条路线均未保留新候选时停止扩训，执行最终比较。
- 正式任务最多60,000局；按完整八轮通常至多55,440局（最终线性复查人数随筛选去重而变）。50GiB剩余磁盘边界保护产物；当前已有约415GiB空闲。
- **2026-09-21 08:00（上海时间）起不再启动新的训练阶段**，允许当前阶段正常完成，然后执行预留的最终2,304局比较。目标是在9:00前留下完整结果；耗时仍取决于实际采样和长局尾部，不把时间目标当完成保证。异常保留日志并终止，不自动吞掉失败。

所有地图仍为已见开发来源、出生不配对。各轮对手可能改变，不能跨轮直接相减。最终新局不再用于自动更新，次日由Agent审核分布、代表性回放、与各自初始化的差异并作发布决定。

## 运行与恢复

入口 `analysis/launch_overnight.py` 复用现有采样、训练、导出与打包器。固定源码、环境、状态、退出码、逐阶段日志、checkpoint及原始对局均在服务器。具体连接和路径保存在忽略的 `work/server-feasibility/active-job.json` 的 `overnight` 项；不要启动重复任务。

当前实现来源 `0849eeb`，已通过Python语法/CLI检查。完整实验不依赖开发机上的浏览器、SSH连接或推理服务。机器重启不在tmux持续运行保证内；若中断，先检查最后的checkpoint/selection与预算，再确定续接阶段，尤其不要在训练截止点后盲目重跑。

## 已验证启动

17:19:37（上海时间）114局端到端预检正常完成，正式批次自动启动。预检包含两个路线各两条训练轨迹、实际PPO更新、多动作导出核对、冻结新对手和最后比较；逐局复算110次正常终局、4次预定tick上限、零异常，角色与计划覆盖一致。记录在忽略目录 `work/server-feasibility/overnight-smoke-audit.json`；小批成绩仅用于功能验证，预检模型没有成为正式任务的初始化。

原启动SSH结束后重新连接，正式96路校准已实际完成91/512局，零异常；独立tmux仍在推进，随后自动测试128路。尚未把未完成的校准当作最终吞吐结论。完整任务的实际状态继续读取 `active-job.json` 指向的远端status和阶段summary。
