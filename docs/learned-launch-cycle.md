# 学习型新波次：实施记录

2026-09-20 12:39开始。用户授权实施既定设计，并使用临时CPU服务器；超过17:30的实验必须能脱离开发机继续运行。玩家默认仍为冻结0.1.16。[设计](learned-policy-design.md)是范围依据，旧的“未启动”是历史状态。

## 本轮交付与边界

落实新波次的候选/选择/提交、完整学习决策记录与模型训练/导出/整局比较。服务器承担采样和CPU训练，采用独立会话、检查点和有界批次；验证原SSH结束后任务继续，不能只把长SSH命令称为detached。远端目录与连接配置只保存在忽略目录，不上传资产、配置、数据或权重。

先保持Legacy行为、验证新控制权，再比较同接口规则/简单策略与模仿/在线学习。观察、候选和掩码保持合法；不把时限当平局，不把训练已启动当作实力提升。按实际结果决定保留何种模型。

## 执行环境

独立工作目录、固定源码、必要MIX资源和CPU运行时已就绪；未同步本机配置或凭证。用户可使用128个CPU，所查CPU配额层级无上限。训练和采样完全在服务器内完成，原SSH退出后多次重新连接确认持续推进；此保证不包括服务器重启。中断后从已完成阶段/对局继续，模型与优化器均保存检查点。

## 首次接入与兼容检查

- `bb8a0df` 抽取LegacyLaunchProvider；139测试通过，短图真实局5,859次与冻结0.1.16同观察指令一致，正常胜出。不把抽取本身当作实力进步。
- 远端Node24.0的同步load hook与tsx组合失败；改为项目私有目录内、官方校验的Node26.5.0。缺少的libatomic从服务器发行版签名RPM提取到项目目录，未改变系统库。
- 明确安装CPU版PyTorch2.9.1+cpu（CUDA=None）及Python3.13隔离环境；首次泛版本解析意外选择GPU依赖已停止，后续固定官方CPU wheel。
- 独立tmux中的固定0.1.16完成8836 tick真实局，约9.45秒，正常胜固定Supalosa；其后远端重放59原快照及终局一致。原SSH已结束后才完成；复制回开发机再核对59快照及终局也一致。

## 新波次接口原型

候选来自合法接触/记忆和独立地图连通性，最多33动作；补了起始存量保留、目标身份及同证据回抽三个控制权边界。146测试通过。`runs/launch-teacher-first` 是新接口规则老师的第一场完整局：136个记录点、29个有选择的点、两次出击，正常胜Supalosa；单局不作强度结论。

实现采用116维事实/四步历史（含历史可用掩码）和32维候选事实，先使用共享MLP对至多33动作打分softmax。与三条件头具有同一完整动作空间和联合概率语义，简化首轮PyTorch/TF.js核对；不宣称取得条件头减少计算的收益。事实编码暂为明确的实体/区域统计与几何，未声称已完成学习型实体编码器。线性候选打分作为同接口便宜对照。

## 首批动作空间检查

远端16局首筛：规则老师6/8、原0.1.16为7/8，全正常；八并发整批约196秒，含一场接近上限的长局，不当作精确吞吐基准。mp08同起点同五辆坦克的首攻，原版目标是GACNST，新菜单却漏掉它而投向GAPOWR；短图合法出生点也被大量远端frontier候选挤出。修正目标压缩：保留建设核心和未探明出生/当前搜索点，已发现目标优先占用剩余槽，再用几何分散填充。未使用旧风险评分筛选。新增回归检查覆盖这两个具体遗漏。

发现连接分区字段每3 tick计算、但学习器仅每75 tick使用；已改为只在学习决策点计算，避免重复图查询。性能收益留待后续相同工作负载测量，不与目标修正的胜率混为一项。

## 首轮学习与兼容结果

冻结源码 `212c8a0` 在独立tmux中执行720局有限序列，任务定位见忽略文件 `work/server-feasibility/active-job.json`。128局老师为74胜、50负、4次上限、零异常；按完整局划分模仿训练/验证，20轮训练。模型为67,330参数的共享候选MLP，四次PPO各用32场新整局、最多三轮更新，梯度/概率校验通过。有限窗口目标为正常胜1、败/达到54000 tick上限0；外部墙钟截断或异常单独排除。

BC权重 `7971ee0e…`。验证决策准确率93.8%，但非KEEP精确动作准确率只有27.3%，故不以总体准确率证明策略能力。BC的PyTorch/TF.js真实多动作样本误差约1.07e-7。后续发现原PPO六样本全部是强制KEEP，原检查没有实际覆盖候选概率；已改为覆盖真实出击及不同菜单大小，并拒绝只有单动作、形状不符或非有限误差的“通过”。四个原PPO模型分别重新验证1/33/33/29/25/22动作的真实样本，最大误差均小于2.05e-7，权重未改。Node与浏览器均使用TF.js CPU同步出口，浏览器在异步大厅初始化时加载/预热，真实工厂保持同步。

四方首比较（每格两局，四图四对手，独立出生）：

| 主体 | 正常胜 | 正常负 | 上限 | 异常 |
| --- | ---: | ---: | ---: | ---: |
| PPO四次小更新 | 20 | 11 | 1 | 0 |
| BC | 18 | 12 | 2 | 0 |
| 同接口老师 | 16 | 14 | 2 | 0 |
| 原0.1.16 | 19 | 12 | 1 | 0 |

对手为0.1.16主线/压力、0.1.8阵地、固定Supalosa；四方对Supalosa都是8/8。PPO对主线5/8、压力3/8、旧阵地4/8。原版本身镜像仅2/8，说明小样本与出生分布的影响不能忽略；不能把20与19的一胜差宣布为提升。地图均已见，未调整正式W/L/U/E判定。

首轮720局已全部完成，程序正常结束。12组线性参数经192局开发搜索选中 `linear-05` 后，独立再比较96局：线性、PPO、BC均22/32胜；线性9负1上限、PPO10负0上限、BC8负2上限，无异常。这个比较没有再次放入原0.1.16主体，不能直接与前表19/32作提升判定。当前未发现复杂模型优于便宜对照的证据，继续固定预算的重复与挑战路线实验。

`runs/learned-bc-shadow` 中实时/冻结模型3,159次同观察指令零差异，正常胜Supalosa，64原快照与停止状态匹配。图形包 `f3f7ae57…` 已实际开局、展开、生产/放置电厂、8:35自然结算、再次开局、主动退出及返回大厅，证据 `runs/player/learned-bc-browser-check.json`；这是功能检查，不是人类水平评测。临时页面和8643服务已关闭。

### 超时反例与分析工具

`runs/learned-bc-cap` 是BC对0.1.16的mp08上限局，240原快照及停止状态匹配。已有720条学习事件中445处有选择，16:10以11单位、33:40以7单位出击，均选建造场。实看14:35的己方基地前装甲交战及34:10的敌方基地前攻击，排除了“从未发起进攻”；停止时仅剩一辆坦克，此前损失35辆坦克和3辆光棱。不能仅凭这些观察认定某次出击导致了全部损耗。

原精简训练日志省略战略计划，通用摘要因此无法展示出击。这次只补充读取已有 `launch_decision` 的一行选择次数/首次/最后时刻，不增加采样字段。该反例现在明确显示两次选择，并保留“选择不等于抵达/开火”的语义；旧日志没有任务仍报告未知。147测试和类型检查通过。

## 扩大实验（用户2026-09-20新增授权）

先用同一冻结策略和地图/对手分布，分别测16、32、64、96路，每块128完整局。记录整块墙钟、引擎tick吞吐、累计CPU和每两秒采样的进程树RSS峰值；包含启动和尾部慢局，随机出生不声称配对。恢复过的块不用于完整吞吐比较，采样数据仍可供同模型PPO更新。只选择实测吞吐最高的并发，校准期间不重叠其他Warbook批次。

随后有界扩展共3,520新局：512校准局兼作原模型更新；两个独立初始化/采样重复各512局；额外32组线性参数各16局并将前四复查64局；八主体各64局比较；最后独立压力路线对冻结新主线和0.1.16采集64老师局、512在线局及128比较局。连同首轮最多4,240局，不无限循环。两个新重复采用相同预算，原模型的512局大更新与它们更新节奏不同，分别解释。

简单对照合计192原搜索＋512新增搜索＋256复查，不给神经方法独占扩容预算。压力仍保留自己的双路步兵和经济承诺，仅共享新波次接口。候选选择用于对手实验，不自动替换玩家默认。全部仍是开发池；若无稳定收益，保留0.1.16和负结果。

## 复现入口

游戏依赖用 `.node-version` 的Node26.5.0和 `npm ci`。训练在独立Python3.13环境固定CPU版 `torch==2.9.1+cpu`、`numpy==2.5.3`；Linux从官方PyTorch CPU wheel索引安装，避免默认解析GPU依赖。设置 `MIX_DIR`，不需要Bot API key。

```sh
# 新目录；命令要在已冻结的git工作树中执行。
python analysis/launch_sprint.py --out /path/to/first-sprint --workers 8 --updates 4 --seed 17
# 首轮完整结束后，固定同一源码/输入，自动校准并发再扩大。
python analysis/launch_expand.py --base /path/to/first-sprint --out /path/to/expanded-sprint
# 仅断点继续时复用原输出目录；更改输入/源码应使用新目录。
```

以上是前台入口。实际服务器由独立tmux会话运行，stdout、状态JSON和结束码都留在远端，环境未包含SSH_AUTH_SOCK或DISPLAY。模型、资产、完整日志/回放和服务器具体目录均不入Git。

第二任务曾在队列中暂停以补足上述PPO校验，实际执行源码冻结为 `046c572`。首轮结束及四模型重新校验后已启动并发校准，原SSH已退出，任务继续。跨学习对手的接线另用 `runs/pressure-frozen-smoke` 验证：pressure老师对冻结PPO完成16,902 tick正常胜局，7.48秒；单局只证明该实验路径能运行。

### 本轮结构与性能检查

在扩大前对 `runs/learned-performance-profile` 做一次完整局CPU采样：本机24,552 tick、10.04秒，累计决策约7.70秒；学习模块及子调用占采样2.08%，本方地图先验12.89%、局部图7.21%。它指出当前模型推理不是该样本主要成本，不代表服务器占比或性能提升。优先校准整局并发，保持既有游戏/规划行为；后续如地图维护确实限制采样，再用同观察工作负载验证具体优化。原profile保存在忽略目录，未新增通用性能系统。
