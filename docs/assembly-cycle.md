# 集结策略续进：2026-09-16

用户授权继续策略研发。本轮保留默认 M0 和可选 counter 的固定玩家包，复用现有分析工具。

## 冻结集结版本复验

计划：[assembly-recheck.json](../experiments/assembly-recheck.json)。两张已用开发图、M0/counter 两个冻结对手、M0/assembly-only 两个被评估版本，每格 4 次，共 32 次启动，全部正常结束。`runs/assembly-recheck` 可用 `npm run analyze` 复算。

| 被评估方 | 城市 / M0 | 城市 / counter | 短图 / M0 | 短图 / counter | 合计 |
| --- | ---: | ---: | ---: | ---: | ---: |
| M0 | 1/4 | 0/4 | 1/4 | 0/4 | 2/16 |
| assembly-only | 2/4 | 3/4 | 4/4 | 0/4 | 9/16 |

这批 M0 镜像被评估方只有 2/8 胜；短图 assembly 实际起点为 1→0 七场、0→1 一场。槽位已经交替，出生位仍不均衡。保留信号与反例，不将 7 场差距直接当成已确认的综合增强。

## 两个不同的败局

- `mp06t2.map/counter/0-assembly`：第四辆坦克在 tick 5886 出现于工厂内部 (76,38)，此时全军已接到出发令。tick 6174 前三辆在前方，第四辆还在 (77,50)。前三辆首次冷却重启为 6201、6234、6300；首辆损失 6320 时第四辆仍未出现开火信号。整局 9012 正常败北。原回放、全部稀疏单位快照及终局通过 `analyze:encounter` 核对。
- `mp03t4.map/m0/0-assembly`：首轮四辆坦克击毁四辆对方坦克而存活，tick 7274 时仍保有四辆；至 tick 9600 才又在造坦克。期间同时建第二矿场、造矿车，最终五辆矿车；后续未把开局优势转成胜局，23322 正常败北。这说明不能把所有败局都归给首次接战人数。资源路线本轮暂不改变，以免混淆出发条件的比较。

## 单项干预：出厂并靠拢后再出发

候选 `formed` 从 assembly-only 派生，只改开局出发判断：至少四辆普通坦克已经在己方建筑 footprint 之外，并存在一辆坦克周围 6 格内有四辆。仍可对附近合法接触立即接战，仍在 tick 9000 结束开局等待，出发后不因损失重新进入等待。

这些是本轮待验证的策略默认值。几何条件不证明道路可达或四辆同时开火；完整局与密集回放用来检验其实际影响。规则只读既有合法己方位置/建筑范围，无新观察字段、隐藏敌情、对手标签或引擎句柄。与开局集结无关的经济、生产、碾压、指令节奏均保持原策略。

记录败局几何的决策测试检查：刚出现的第四辆仍在厂内时等待、加入后出发、阵亡不重置、分散时等待及原有时间上限。机制测试与真实完整局分别报告。

## 干预筛选结果与反例

[formation-screen.json](../experiments/formation-screen.json) 固定了双方代码后启动 32 场，全部正常结束：assembly-only 10/16，formed 13/16。分格依次为短图 counter 2/4→2/4、短图 M0 4/4→4/4、城市 counter 1/4→3/4、城市 M0 3/4→4/4。`formed` 包 SHA-256 为 `9ea48b5fcd935272c2f65331d61af0eceef97b5a5050e298e423a88b6ce18222`，来源 `6ab5c9f`。

两个短图候选回放都核对通过，但说明了不同边界：

- `counter/1-formed` 在出生位 1：四车首次冷却重启依次为 6240、6267、6306、6402，己方首次坦克损失 6536。第四车能在首车损失前参与；tick 6615 时对方首轮四车已损失，己方仍三车，最终 13716 获胜。普通新局没有相同初始化，不能把它当成旧败局的精确反事实。
- `counter/0-formed` 在出生位 0：出发约 tick 5949，四车首次冷却重启为 6213、6252、6312、6606，首辆损失 6328。最早生产的坦克在行军中落到后方，最终 11108 败北。出厂门槛已改变实际行为，但不能保证路径上的队形，也未消除逐批接敌。

保留 formed 进入较大冻结比较，不因筛选分数直接发布。经济未改，几何检查仅在开局等待期执行。27 项现有及针对性 TypeScript 测试、类型检查通过。

## 固定开发池比较：收益与实际退化

[formed-pool.json](../experiments/formed-pool.json) 在结果产生前固定：M0 与 formed，三张已用地图、M0/counter/固定 npm Supalosa，每格 12 次，总计 216 次。保持同一胜负和异常口径，全部结束后统一比较；不根据中途分数延长或截断。

现有矩阵脚本只增加了将 `native: supalosa` 转成 runner 已支持参数的分支，复用原调度、台账和报告。它解决这一次固定池必须纳入外部锚点的具体需求，没有新增对手框架或改变评分。实际来源继续由每局 manifest 核对。

216 次全部正常结束。`runs/formed-pool/analysis.md` 的原始结果：

| 被评估方 | M0 | counter | Supalosa | 总计 |
| --- | ---: | ---: | ---: | ---: |
| M0 | 15/36 | 6/36 | 36/36 | 57/108 |
| formed | 35/36 | 21/36 | 29/36 | 85/108 |

新候选对旧主线的直接交手为 35:1，但 `mp29u2.map` 对 Supalosa 从 M0 的 12/12 退为 5/12。总分提升不消除这个已观察的退化，**formed 不替换默认玩家版本**。

首个该切片败局 `mp29u2.map/supalosa/0-formed` 的 replay、原快照和终局已核对：先攻击本地接触（包括已被对方占领的油井），四车在 tick 6000 都已出厂，却未满足 6 格靠拢。tick 7200–7800 返回基地后，还有一车落在己方其他建筑的矩形范围，严格门槛仍不满足；至 8100 才全部满足。之后再出发时，对手已有更多坦克，最终 13188 败北。这是位置条件与机会成本的实证反例，不能继续用“更严格集结总会更好”的解释。

## 最小替代：仅检查出厂

候选 `factory-exit` 从同一基线分支，只要求四辆坦克走出己方车辆工厂的矩形范围。取消全军 6 格靠拢及对其他建筑矩形的排除；已经接战的部队因此不需要返回开局集结点。经济、合法观察、目标、时间上限保持原样，不读取地图名或对手身份。

来源 `b34abc5`。[factory-exit-screen.json](../experiments/factory-exit-screen.json) 预定 48 次：assembly-only、formed、factory-exit × 城市回归/短图 × Supalosa/counter × 每格 4 次。原 formed 保持冻结，第一张新地图暂不使用。这样先定位退化，再由新比较决定是否保留简化条件。

新地图已在策略比赛之前选定为 `mp08t2.map`，仅完成 tick 0 加载/布局检查：120×120 世界范围、两个起点、同一坦克可达区域、起点间 56 节点路径。另一个可枚举自定义地图文件本机缺失，加载错误保存在 `work/formation-new-map-inspection.json`，没有策略胜负。原 [formed-new-map.json](../experiments/formed-new-map.json) 因候选退化暂不执行，不用改后的候选冒充原冻结版本。
