# 动态防守与有效进攻：2026-09-17

11:04 起按用户“可以动手改进”执行 [修正计划](defense-control-plan.md)。维护入口继续用冻结包，工作分支 `codex/active-defense`。只用本仓库和固定官方依赖。

## 基线与第一项修改

- `runs/active-defense-start`：最终 0.1.8 bastion 对 Supalosa，短图出生位 1，8804 tick 正常败北，重现原失效。
- `c91728d`：共享护援点给步兵，保护对象扩大到所有己方建筑及矿车；建筑受损时可中断反击回防。已有敌情更新接近方向，取消首次矿车位置对方向的永久覆盖；GI 根据射程部署、解除部署和攻击。经济目标保持原样。观察只增加己方武器射程，不扩大敌方可见范围。
- `active-defense-screen` 固定 16 次，两图、旧主线/Supalosa、两个 subject，每格两局，全部正常。新旧各 5/8；新版本小镇对 Supalosa 2/2、短图对 Supalosa 1/2，短图对旧主线 0/2。起点未控，作为行为筛查。
- 新版本短图出生位 1 的胜局 15220 tick，但默认行为摘要仍指出 5:00–5:29 守军没有火力。这段发生在首批步兵前出后已严重损失的阶段，不能因最终获胜而忽略。
- 原回放/命令进一步核对：首批 GI 确实部署过，但会主动追逐距离保护建筑尚远的目标，先后进入敌方火力范围并被击杀。第二项限制远处追击：根据可见对手已知武器射程与保护对象距离决定是否需要接战，已能开火的单位继续射击；预备岗位改为沿接敌面排列。此项只增加当前可见单位的已知武器射程字段，不读取敌方目标或冷却状态。

后续先复验步兵实际参战与保护效果，再用已探索通路改进预备位置，并验证完整目标交给原生路径后能实际进攻。不得将已下令、阶段名或新胜局当作行为验收。

## 后续干预与保留结果

| 试验 | 冻结来源与改动 | 实际结果 |
| --- | --- | --- |
| `defense-frontage-screen` | `f85f2df`：只追击已能威胁保护对象的敌人，已在射程内的目标仍可交战；步兵沿接敌面展开 | 16 次正常，新候选 6/8，`c91728d` 为 5/8 |
| `defense-route-screen` | `789c3b5`：由桥接层用 SDK 路径在已探索区域选预备点 | 16 次正常，路由候选 6/8，`f85f2df` 为 7/8。短图路由候选两场 Supalosa 都出生位 0，对照覆盖不同，不据此单独判定收益 |
| `defense-crush-screen` | `bdc5d13`：责任区内近六格的步兵可被碾压，若更近处有敌方坦克则先交战；其余沿用路由候选 | 16 次正常，新组合 8/8，对照 6/8；短图对 Supalosa 的出生位 0、1 均有正常胜局 |

组合胜局 `defense-crush-screen/mp06t2.map/supalosa/0-support-crush` 的原回放与 108 份快照核对一致：4:30–4:59 有六名步兵开火信号，两秒窗最多五名，之后 13:31 六车反击、14:16 起敌方非防御建筑受损，17:55 正常胜利。该例支持实际防守参与和进攻，不把小样本 8/8 当成通用实力保证。

地图辅助复用 SDK，不实现另一套寻路器。端点、路径节点均限制为己方已探索地块，关闭自动迁移不可达终点的 best-effort；只输出预备点坐标，不输出全图路径/连通编号。每 150 tick 最多六次查询，实际试跑中有路由观察记录。相关行为和不可透传未探索路径的检查已加入测试。

### 历史移动原型的单独检查

在本仓库的临时 Git worktree 从 `4f107e7` 仅把直线短途点替换为完整目的地，冻结为 `4ab2073c8e275637e1a09cf46c72ded47d70a3dc`；worktree 已移除，研究分支保留。`native-path-screen` 八次启动：旧版 0/4（其中一次上限）、修改版 0/4（全部正常败北）。**修改版四局都在出现进攻任务前败北，因此这批没有验证该行修改的交战或强度收益。** 未将“上限减少”算作修复成功，也没有把原型恢复到当前运行代码。

当前发布组合继续使用已有的完整目标 Move/AttackMove/Attack 原生命令，真实完整局验证了离开基地、损伤敌方建筑和结束比赛。复杂的集群移动射击仍未发布。

## 0.1.9 固定池

最终行为来源 `c1bfcffb85e0e588ab364989d6c4f6cd8998c7c8`。相对 `bdc5d13` 只改版本及组件标识，发布检查两场正常，10,131 次同观察决策无差异。

`active-defense-pool`：四张已用地图、固定旧阵地 0.1.8 / counter / Supalosa，各格四次，同时重测旧主线与新版，共 96 次，全部正常结束。

| 被评估方 | 对旧阵地 | 对 counter | 对 Supalosa | 总计 |
| --- | ---: | ---: | ---: | ---: |
| 0.1.7 | 6/16 | 6/16 | 13/16 | 25/48 |
| 0.1.9 | 9/16 | 16/16 | 14/16 | 39/48 |

按小镇 / 短图 / 城市 / mp08，新版为 11/12、12/12、7/12、9/12，旧主线为 6/12、9/12、5/12、5/12。新版短图两种出生方向各六局；其他切片仍有起点分布差异。城市对旧阵地 1/4、对 Supalosa 2/4，mp08 对旧阵地 1/4，保留为下一项反例。此池不同于 0.1.8 当时使用的池，不能直接相减旧报告的总分，也没有未见地图或真人强度结论。

结合行为修复、完整局结果和玩家检查，将 0.1.9 设为默认，0.1.7 保留可试玩；旧阵地、counter 和其他历史版本继续冻结。

## 发布与运行核对

- Node 包 SHA：`7bbe43fc0dc7765a4a722ab0e2b2a269f8a96624bc9b694758e25f79e1c25fd4`，源码 `c1bfcff`。
- 图形包 SHA：`3d784b5a262f1b74da8069dc392565d76c5b6cbad80e872362ba59073f93cafe`，构建提交 `dd8e0ae51558843fb199392bfc2660c8ee4c9f5c`；相对行为来源只新增实验计划，构建前确认相关代码无差异。
- 旧主线在新增观察字段下的单场完整兼容检查正常结束，4,274 次与冻结 0.1.7 的同观察决策无差异。61 项 TypeScript 测试、类型检查与构建通过。观察允许范围说明更新在 [接口契约](interface-contract.md)。
- 图形客户端实际完成进入、鼠标/键盘展开、生产并放置电厂。第一局以速度 6 正常结算，界面 15:17，Game.onEnd tick 13459，player_exit 13760；这两个边界不混写。第二局恢复默认速度 4、重开并在 0:31 主动退出，tick 472 时仍 Started、双方未败北，不计正常败局。
- GUI 记录 `runs/player/active-defense-browser-check.json`：两个 game_created，94 份带路由的观察，组件 `bastion-strategy-v7 / position-tactics-v5`，无记录到的运行/提交错误。
- 12:03 正式本地 8642 已切换默认 0.1.9、对照 0.1.7，维护 PID 75850。三个 HTTP 内容哈希核对通过；正式首页与两个客户端版本标识也已验证。图形构建现在保留相同字节的原来源元数据，不在文档改动后的重建中覆写已冻结包。
- 默认回放 `active-defense-release-check/mp06t2.map/supalosa/0-release`：出生位 1 的新版对出生位 0 的 Supalosa，15492 tick 正常胜利，资金 991/281，SHA `d58f39f549cedcddce0203dba28959ecb2ec2357006a30b130c45559fbf1c855`。原回放、104 份快照及终局核对通过，4:30 防守窗口六名步兵参与，13:19 六车反击。图形回放检查到 0:13/17:12；完整终局由 Node 核对。

本轮共 172 次 headless 启动：171 正常结束，一次来自未采用历史原型的运行上限。跨版本和用途，不合并成一项策略胜率。临时服务器、worktree 和测试页已关闭，完成实例已释放，只保留维护入口。原资源、回放、截图和本地配置未进入 Git。

本轮 11:04 开始，12:03 切换正式服务，之后完成入口核对与记录。后续活跃任务的 24 小时检查点预计以 1–3 小时定位城市旧阵地反例并做小对照；72 小时检查点预计以 3–8 小时完成相应修正/扩大复验。它们是滚动工作量预测，不承诺胜率或连续后台运行。
