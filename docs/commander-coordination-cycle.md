# 完整战略：计划协调、采样与学习起点

2026-09-24。承接[过夜早停收取](commander-cycle.md#2026-09-24收取首轮后中断没有完成过夜训练)，保留完整战略权限、主线与压力两路线；稳定玩家版仍为0.1.16。下述地图均是已见开发来源，出生位未精确配对，不能当作未见泛化或组件收益的精确估计。

## 从矿车误派开始排查

同一批真实观察，分别给成员分配头输入教师的完整上游选择和模型自己的选择。原诊断同时改变了生产与任务条件，不能把全部差异只归给任务头：

| 路线 | 矿车任务类型正确：教师条件→模型条件 | 坦克任务类型正确：教师条件→模型条件 |
|---|---:|---:|
| 主线 | 336/342→262/342 | 361/364→294/364 |
| 压力 | 430/497→443/497 | 570/715→378/715 |

每路线四份完整DAgger来源，条件诊断不等于闭环胜率。主线确有上游计划与下游成员协调问题；压力矿车没有同样下降，不能统一归因为成员头容量不足。另一个标签身份检查发现，两路线分别416/989次教师矿车改派中，没有“只换槽位、实际任务不变”的情况，因此暂不做大规模任务槽位匹配重构。

已修复实际编码错误：相同坐标下，原生采矿目标与固定位置目标被当成同一目标；`3c9e5ac`开始保留native身份。后续实验的世界编码源相同，不混用修正前后的训练输入。

## 学习何时修改计划：本次候选失败

`9ab0839`增加五个可学习二元选择，决定本次是否重审生产、任务、成员、建筑操作和建筑落点。关闭时保持计划，打开时保留原完整动作空间；只增加645参数，没有矿车禁令或规则战略补救。

采集256份新完整局，每路线64教师局与64教师混合DAgger局。四个候选从各路线相同共享权重出发，用相同128局数据、相同600次BC更新比较原v2与新v3。教师/混合采样成绩不作独立策略强度。

256场纯模型比较，每项16局：

| 压力候选 | 采样胜场 | 贪心胜场 |
|---|---:|---:|
| v2起点 | 0 | 5 |
| v2更新后 | 0 | 6 |
| v3起点 | 0 | 4 |
| v3更新后 | 0 | 0 |

主线八个对应条件均0/16。新门控候选在展开基地后关闭生产修改，漏掉第一座电厂；同一份v3权重去掉门控后，额外32局主线0/16、压力4/16。因此不晋升门控方案。这里的6比5也不是已证稳定改进：胜负在不同对手间有得有失。

数学/执行检查与强度分开：Torch/TF.js迁移概率核对通过，50个真实记录状态重新计算实际潜变量联合概率，最大误差小于4e-6。Pro复核提醒原条件诊断被关闭成员门造成的必选KEEP夸大；`5d06bcd`已分别测“打开成员头后的预测”与“实际门控策略”。真实v3策略必须记录五个二元决定，PPO不能从最终动作反推潜变量。咨询原文留在私有目录，代码判断已本地核对。

## 采样噪声：有可用起点，但尚非RL收益

v2压力贪心能赢而温度1采样多败，下一项只改采样温度，保持全部模型权重、权限与观察不变。`f13e9e9`将实际温度写入决策和模型；Torch与TF.js按同一分布计算概率，PPO拒绝混温度数据。小温度使用稳定对数概率计算，不能把极小概率裁成固定常数。

240场新比较，每项16局：

| 模型 | 温度1 | 温度0.5 | 温度0.2 | 温度0.05 | 贪心 |
|---|---:|---:|---:|---:|---:|
| 主线BC更新后 | 0 | 0 | 0 | 1 | 0 |
| 压力BC更新后 | 1 | 5 | 7 | 3 | 5 |
| 压力原起点 | 0 | 1 | 4 | 4 | 4 |

池为Supalosa、对方路线0.1.16规则、对方路线冻结完整战略v2、旧强窄权限MC83，四地图各一次。压力更新后温度0.2的7胜分布为1/4、1/4、4/4、1/4，不能忽略四场弱同伴胜利。主线温度0.05唯一胜利也来自弱同伴。

十二份温度模型的共享张量与各自父模型逐项相同，导出核对、贪心不变和低温度有限梯度检查通过。784场已完成的采集/正式比较均从原始manifest/result重新分类核对，0错误（另有前期四场预检独立保存）。**当前结论是压力线获得了可尝试PPO的随机策略起点，不是RL已提高实力。**

## 当前执行与假期安排

约12:35已启动冻结`f13e9e9`的独立实验：压力线温度0.2采集192场从零开局，用同一数据分别做种子47/83的64次PPO更新，再与原起点同池比较；主线温度0.05比较0、4500、9000tick教师前缀后接管，各32场。前缀沿真实引擎推进，不是快照恢复；教师行动不计作PPO动作，纯模型整局仍独立评价。

服务器任务`jobs/commander-ppo-prefix-20260924`，tmux `warbook-ppo-prefix-24`；源工作树`jobs/commander-temperature-20260924-src`不可热改。私有定位和脚本在`work/server-feasibility/active-job.json`与`work/commander-coordination-20260924/`。固定旧规则、强历史及当代两路线仍保留；经典法国打法没有进入示范、奖励或专门课程。

假期87.5小时任务尚未部署。先用当前试验决定训练方式，并在24日17:30前检查独立运行、空间预算、失败隔离和最终比较。不能原样把早停/零胜路线续跑三天，或把脚本离线运行说成Agent会自动改变研究方案。

## 13:20前新增证据：纯学生状态纠错有效，首轮PPO尚无收益

压力T=.2新192局为73W99L20U、0E；对Supalosa/对方规则/强历史/弱同代分别17/48、7/48、10/48、39/48。原始106状态的实际联合logp重算最大误差2.69e-5。47/83两个优化重复使用同一192局，各64更新、同起点；新评测每项32局，原起点16W12L4U，更新后11W16L5U、9W18L5U。它们是优化重复，不是独立环境训练重复。

在另取的64局、每局五个阶段共320状态上，最终候选相对行为策略的条件KL估计为.0196、.00985，裁剪范围之外比例30%、26.3%；这是记录hidden条件下的检查，非新权重完整循环运行的KL。第二次退化没有对应超过.02的最终平均值，不能直接把两次失败都归因于更新过大。另16局48个序列窗口的局部梯度检查，critic/actor范数比中位.074、12批中10批方向余弦为正；降低“价值梯度普遍压倒actor”的优先级，不证明不存在局部干扰。

主线前缀0/4500/9000tick各32局为0/0/3胜，最后三场只在接管后812–2284tick结束，不能当作自主中盘能力。直接看了短图败局5:30、6:30；同时日志确认资金为0时唯一矿车仍在执行侦察敌出生点，首坦克6:30才出现，8:40败北。临时实例已关闭。

随后新增128局β=0 DAgger：教师仅在模型实际局面提供纠错标签，零教师操作；0W107L21U，0E。从同一主线BC权重、T=1监督、同600更新比较继续旧128局与旧128＋新128局，各47/83两个优化重复。统一T=.05及贪心的新320局结果：

| 条件 | 采样胜场/32 | 贪心胜场/32 |
|---|---:|---:|
| 未更新起点 | 0 | 1 |
| 继续旧数据47 / 83 | 0 / 1 | 0 / 1 |
| 增加纯学生状态47 / 83 | 5 / 2 | 6 / 9 |

恢复83贪心的9胜分别来自Supalosa3、对方规则4、强历史1、同代1；恢复47贪心的6胜分别为0、2、2、2。支持新增访问状态的纠错价值，但未晋升玩家，也不因此宣称已具备稳定RL起点。地图已见，训练数据与开发诊断无独立holdout保证。

`d63f760`把诊断改为生产×任务四种条件，按合法掩码判断任务是否存在，避免低温度概率下溢影响可达性定义；精确语义另包含交战参数，不直接与旧精确分数相减。在16份新DAgger来源中，初始→恢复83的577份矿车决策：模型条件下正确390→475；任务不存在75→92；已有合法任务却选错112→10。教师生产与模型生产的同任务结果接近，问题主要落在任务生成。恢复后的102份矿车错误中92份缺少采矿任务，因此暂不实施新的成员辅助损失，优先下一轮上游计划纠错。该模型已见这些来源，这是诊断，不是泛化验证。

第3次Pro复核已完成并本地检查以上条件链、概率和梯度；私有原文`work/consult-pro/strategy-discovery/reply-03.md`。当前服务器正在做`commander-curve-20260924`：同一压力更新分别保存8/16/32/64步、每项64局，最后64步张量与原实验逐项完全相同。其主线极低温度支线在数值预检停止，未运行对应对局；压力支线继续。失败来自联合logp累计误差4.90e-4，概率最大误差仅2.21e-6；`e9ab3d5`将联合似然比误差界限设为.1%，概率/hidden/value仍为原1e-4，并在新`commander-calibration-20260924`复查原十二份控制后比较主线T=.05/.01/贪心。更低T=.002不进入本次比较，不能把未运行算失败对局。

新采样默认采用经过实测的无损zstd，见[数据保存](experiment-storage.md#2026-09-24完整战略日志的压缩比较)；四份真实episode共865决策在Python训练读取、Node解压和恢复后完全一致。200项Node、类型检查及6项存储检查通过。假期长期作业仍待当前学习曲线结果确定，不恢复昨日失效任务。
