# 阵地防守与持续成军：2026-09-16

14:30 根据用户“继续”开始实施 [玩家经验](player-feedback-0.1.7.md)。稳定入口保持 0.1.7，先形成能惩罚逐辆进攻的守势挑战者，再决定主线的编组改动。

## 第一项候选：bastion

初始假设与可检验行为：

- 步兵始终归属基地守备任务，到岗后 E1 部署；坦克单独集结。岗位来自己方基地、公开起点和首次合法可见近处敌军，不访问隐藏状态。
- 沿用 0.1.7 的经济目标和矿车承诺记账，车辆工厂建成后另买一座碉堡/哨戒炮，费用来自正常收入。现有已探索范围的建筑放置入口保持不变。
- 反击由八辆已出厂、位于基地集结范围内的坦克触发；15000 tick 后可由四辆触发。主力成员持续保留，剩两辆坦克时回到集结阶段；后续增援先积累四辆，成为独立汇合任务。
- 第一轮出击仍使用原局部作战控制，尚未声称能持续保持行军队形、轮换承伤或实现 T 优；防守组合与这些后续战术分开。

这些数字是首轮候选参数，不是玩家给定规则或已验证的最佳值。主要风险是岗位不在实际交火方向、步兵反复部署/移动、工事挤占补兵、反击过晚或增援无法汇合。需要从真实行为和完整局决定下一步。

## 为实际分工补齐的控制边界

在原主力任务旁增加有明确单位归属的附加作战任务；任务 ID、修订、效果反馈及旧命令拒绝覆盖全部任务。守备需要原生 Stop，加入这个动作，不能用不断下发 AttackMove 冒充原地防守。不同任务间转交时清理该单位的战术命令缓存，避免旧“已下令”状态阻止新的停止/出发指令。

原 `factory-exit` 保持默认算法；先做少量同观察指令比较确认接口扩展没有改变旧策略。新策略不要求与旧策略输出相同。

## 第一轮预算与决定

1. 正常零起始军队对局 `runs/defense-start-check`：冻结 0.1.7 对 counter，12836 tick 正常获胜，只验证现有运行入口。
2. 初始 bastion 在小镇/短图对冻结 0.1.7 各两次，交换创建槽位，共四次；先检查部署、工事、集群反击和结束，而非宣告强度。
3. 有效防守压力形成后，冻结候选，在包含旧主线、counter、外部 Supalosa 的相同条件下比较；必要时拆除工事/部署做归因。若组合未形成，不用更多局数替代定位具体控制问题。

保持现有 54000 tick / 180 秒上限、正常胜负与异常口径，所有启动保留。新对手引入时同时测旧主线，不把不同对手池的总分相减。完成的实例立即释放，不启动后台常驻比赛。

## 已发生的行为与失败

初版来源 `382951b`。44 项测试通过，原 factory-exit 的两场完整影子比较正常结束，7,694 次指令无差异。bastion 初始四场全部正常败北，不能将部署和工事出现等同于有效新对手。

实际四场都在 tick 3552 出现碉堡，最多六名大兵部署；Para、Vulcan2 和坦克炮都有击毁敌方坦克的记录。小镇 0 号局的原回放、稀疏快照和终局核对通过：守方挡住第一波，第一辆坦克到 13939 才损失；但 12000 时矿车在 (35–36,75–78) 受袭，主力留在 (26–35,87–95)。13500 时七辆坦克仍在，矿车已为零。固定岗位和过晚出击没有保护经济，不能据此否定玩家指出的防守优势。

## 两因素对照

[bastion-factors.json](../experiments/bastion-factors.json) 预定 32 次，全部正常结束，固定对手 0.1.7：

| 候选 | 来源 | 小镇 | 短图 | 合计 |
| --- | --- | ---: | ---: | ---: |
| 八车、固定岗位 | 382951b | 0/4 | 1/4 | 1/8 |
| 六车、固定岗位 | f11a1f0 | 2/4 | 1/4 | 3/8 |
| 八车、机动护矿 | cd9446b | 1/4 | 0/4 | 1/8 |
| 六车、机动护矿 | da3958a | 2/4 | 2/4 | 4/8 |

机动护矿仅根据当前可见敌军与己方矿车/矿场/建造场的位置响应，步兵仍守基地；90 tick 更新支援点，敌情消失 300 tick 后返回集结。普通起点未控，不把小样本差距直接当成因果或稳定实力。

组合尚未形成足够强的惩罚，因此继续检查战术。固定岗位控制让车辆只能站到预定格子并自行索敌，难以共同支援。下一项仅将防御车辆改为共同攻击责任区域内的可见目标，优先作战车辆与低血量目标；目标离开该区域或视野后切回移动/停止任务。反击战略和生产仍固定在 da3958a。

该战术候选来源 `a4fc8be`，[bastion-focus.json](../experiments/bastion-focus.json) 八次均正常结束：共同追击候选 1/4，原位置控制 3/4。短图车辆离开支援位置接敌，未采用这项战术；保留冻结版本，不将“共同目标”直接等同于好的防守。

## 防御位置、近距自卫与反击窗口

- `5afd05d` 让防御工事朝己方实际采矿路线放置，并把守备点绑定到实际工事。桥接层只使用战略给出的普通坐标，在原有已探索、己方建筑附近的合法 footprint 中选址，没有扩大隐藏观察。`bastion-line` 四次正常，1/4 胜；没有把单项选址当成已确认增益。
- `a580008` 恢复有限近距作战：车辆只显式攻击距自身 6 格、责任点 12 格内的当前可见目标；只追碾自身 3 格内的步兵。区域外转回岗位。原全区域追击版本不再使用。
- `ca27135` 增加反击窗口：曾看见至少两辆近区敌方作战车辆，随后可见压力降到不超过一辆且保持至少 90 tick 时，允许四辆就绪坦克反击；正常门槛仍为六辆。它依赖当前可见压力，**不将敌人离开视野认定为阵亡**。
- `bastion-window` 八次均正常：仅近距自卫 1/4，加入窗口 3/4。由此将窗口组合冻结后扩大比较，不宣称这四局证明最佳参数。

## 固定池：形成专长，而非替换主线

[bastion-pool.json](../experiments/bastion-pool.json) 四张已用地图、0.1.7/counter/Supalosa，每格四次，两版本共 96 次，全部正常结束。

| 被评估方 | 对 0.1.7 | 对 counter | 对 Supalosa | 合计 |
| --- | ---: | ---: | ---: | ---: |
| 0.1.7 | 11/16（镜像） | 13/16 | 14/16 | 38/48 |
| 阵地反击 | 12/16 | 13/16 | 3/16 | 28/48 |

阵地候选 Node SHA 为 `99fe5289bf3efed7dd4ad79a4a70c24609fa74e0205e0dcd939efbcd147af169`。它对旧主线提供了实际压力，却在外部对手上明显弱；这批呈现路线间克制，不能按总分或同源互战宣布全面增强。决定保留为专门挑战者，默认主线仍用冻结 0.1.7。镜像结果、起点不均衡及每地图成绩保留在原报告中。

## 主线编组实验与负结果

1. `4f107e7` / `cohort`：保留守家步兵、反击主力和四车增援批次，尝试共享短途移动、途中收拢和接敌后的移动射击。`cohort-screen` 候选 0/8（5 正常负、3 上限），同批 0.1.7 为 4/8，八局均正常。上限仍 Started、无 turn-manager 错误；两个终态分别有 53、60 辆坦克，却未能完成攻坚。该实现会将援军不断并入单一大队，中心/距离控制不能可靠处理其规模与地形，不能把“能移动开火”升级为已具备集群战术。
2. `63ec881` / `cohort-local`：固定编组战略，恢复原局部战斗控制。`cohort-local-screen` 两方各 6/8，全部正常；新候选对阵地反击 4/4，但短图对 Supalosa 0/2，旧主线该格 2/2。没有晋升主线。
3. `648b888` / `cohort-local`：推迟经济扩張直到首批六车实际发起任务，另外修复全灭后的成员回收。`cohort-budget-screen` 候选 3/8、旧主线 6/8，全部正常。该方向也未显示稳定收益，不继续微调相同预算假设。

失败的单一大队移动实现及其特定测试已从当前运行代码移除，Git 和完整实验仍可追溯至 `4f107e7`。`cohort-local` 仅保留为研究模式，不用于玩家默认对手。后续需要从有限小队、射击窗口、侦察和实际交战反例出发；不能用更密的命令或更大的聚拢半径代替战术。

本轮结构检查发现主力在一次观察间隔内全灭时，原过滤顺序会漏掉“重整”分支，让未汇合援军滞留在旧任务中；已以保留过滤前状态修复，并增加全灭/援军回收测试。47 项当前测试通过。未发现需要通用调度平台的阻碍，未做不等价的性能提升声明。

## 最终阵地包与玩家检查

最终行为来源 `648b8882571f6c80c462dfbf9314cee7f2f18da3`，版本 `warbook-0.1.8`、mode `bastion`。Node SHA `e8d575b46a453446ce5b4bec0d5848a4deed8316219ed9fd29c793807fc5a54b`，图形包 `6ea486030eebc01643999995731a754668d252d8767c14030635e4f8bce68e3c`。对已评估候选只增加全灭成员回收修复，其他改动为元数据或未选择的研究分支。

`bastion-release-check` 四局正常结束，与 ca27135 的 18,823 次同观察指令比较无差异；没有声称该恢复分支在这四局实际触发，其机制由针对性测试覆盖。

选定最终包对 0.1.7 的小镇对局 `runs/bastion-release-check/mp03t4.map/published/0-release` 作为下一份观战样本：阵地方在 18612 tick 获胜；3552 出现碉堡，最多六名大兵部署，10386 四辆坦克反击，11883 首批四车援军出发，之后还有四车批次。原回放已重放到相同 tick，资金 1471/100 与败北状态一致，SHA `f843ad9a2c02ad0ae5edc1e4e4e41e20a6965d3ca1cfc8a5806a4c043c049348`。

候选玩家页已从首页进入 `/challenge/`，首局实际鼠标/键盘展开基地、14:05 正常结算；Game.onEnd 12531 / player_exit 12682 分开，客户端确认六名部署守军及 `bastion-strategy-v5`。重开、建造、退出和正式入口切换仍在完成中，尚未把候选页当成已发布入口。
