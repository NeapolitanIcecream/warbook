# 假期实验：完整战略的持续纠错与RL对照

**2026-09-25 04:54已正常完成并释放服务器，早于调整后的26日11:30期限。** 冻结运行源`0058aa088adc4046c2079f67e466f8abcb2eb762`，稳定玩家入口仍0.1.16。任务于24日16:07独立启动；以下计划保留为运行协议。

## 25日快速检查：已提前完成

11:41核对208份批次摘要与实际进程：28,096场，23,854场明确胜负、4,242场游戏时长上限、0程序错误，全部批次完成。主任务与资源监督器退出0，25日04:54:37释放，无我们的残留进程。没有触发26日的时间强停：主线PPO47在24轮后，其余五线在12轮后，均因连续两段未达到规定增益门槛而停止。该分配规则不证明已经收敛。

最终四固定对手池各64场，初始→保留模型胜场如下；四轮final及64场路线交叉比较均完成。

| 线路 | seed 47 | seed 83 |
|---|---:|---:|
| 主线BC | 18→13 | 22→25 |
| 主线PPO | 20→19 | 23→30 |
| 压力PPO | 20→24 | 18→18 |

固定对手为Supalosa、两条0.1.16规则路线及旧强MC83，四张已见地图，出生未配对。主线PPO47的最新/段级参考另为16/64、14/64；压力PPO83最新为10/64，不能只报告保留点。当前看到部分候选收益，尚无跨重复稳定、持续提升的结果。此次只核对批次摘要、退出记录与资源状态，完整逐局模型/产物及回放审计仍待后续收取；私有快照`work/commander-coordination-20260924/holiday-status-20260925.json`。服务器已交还，不自动启动新任务。

## 今天得到的依据

- 纯学生访问状态纠错帮助主线进入可用状态。首轮同池各64局，47由7→20胜、83由10→25；固定三控制分别5→11/48、6→18/48。
- 强父模型上的新192局对照，每项32局：47父模型13、继续BC8、PPO12胜；83父模型11、BC14、PPO15胜。固定三控制上，PPO分别10→11/24、7→11/24，合计17→22/48。样本有限，是进入长期对照的正信号，不是已证稳定提升。较弱父模型的另一次64局PPO试验退化，两个起点分开解释。
- 压力short47最终四固定对手池由14→24/64，83为16→16；较小学习率、多更新的small线路没有一致收益。保留其失败和模型，本次将两个活动位置用于主线PPO。
- 384局BC缓存曾被内核OOM终止。修复后，同24局/8697帧峰值5.13→1.70GiB，355万输入元素、4次BC更新及16rank八步PPO完全一致；原两条384局/600更新补跑完成，新增192局评测0E。新任务使用紧凑数组与四NUMA节点交错分配，不降低数据精度到模型原本输入以下。

完整过程、负结果和对手分布见[计划协调](commander-coordination-cycle.md)与[日间共同演进](commander-joint-cycle.md)。全部地图是已见开发来源，出生位未精确配对，不称作未见泛化验证。

## 六条活动线路

| 线路 | 起点与训练 | 部署分布 |
|---|---|---|
| main-bc-47 / 83 | 各种子的强主线C0；旧128局锚点＋最近两批实际状态；每轮600次BC | BC以T=1训练，部署T=.01 |
| main-ppo-47 / 83 | 与对应BC相同权重；每轮新128局、8次PPO、学习率5e-6 | T=.01 |
| pressure-ppo-47 / 83 | 日间short线路第二轮完整模型；每轮新192局、8次PPO、学习率1e-4 | T=.2 |

主线每个种子的BC/PPO首批共享已有的128份全程学生操作轨迹：BC读取教师查询标签，PPO只读实际动作和结果。两种子合计256份历史实局，重复使用不算新采集；首批当时的对手版本由原manifest保存。以后每条线独立采集新轨迹。BC每轮重置Adam；主线PPO明确冷启动，压力PPO带日间Adam继续，此后PPO要求对应优化器存在并记录哈希。

学习范围仍是完整v2战略，固定战术负责执行；没有新增规则救援、成员禁令或法国经典打法示范。BC仍使用已有规则教师，故整个环境包含教师辅助；PPO线路不读取新增教师标签，但不能把整个实验称为没有教师先验的封闭自举。

## 对手、保留和停止

每轮开始冻结对手。采样池为Supalosa、对方0.1.16规则、强旧窄权限MC83、当前对方学习路线。主线面对同种子的压力模型；压力交替面对主线BC与PPO。冻结对手贪心执行，训练actor按表中温度采样。

每轮将候选与retained同池各评64局，先比较三固定控制，再比较全池。current继续有界更新，retained单独保存，不因一次有限样本退化中断学习。

**每六轮**，current、retained与段级参考进入新四固定对手池（Supalosa、主线0.1.16、压力0.1.16、MC83），去重后各评128局。至少多8胜才更新段级参考；这是计算预算分流阈值，不是统计显著性声明。连续两次段级复核都未达到该增益，就停止该线更新，保留其retained作为对手。其他线路继续，资源重新分配；不自动改奖励、温度或控制范围。

最终固定池分轮均衡覆盖所有线路的initial、retained、最新已验证模型及段级参考，随后做两路线交叉对战。保留所有结果，胜率提升不自动等于发现新打法。

## 资源与交接边界

- 最多40轮、8万次**新启动对局尝试**。采样、候选比较、段级复核、最终矩阵全部占额度；错误和被中断的尝试也计数。下一轮和最终比较先预留额度，实际可能少于40轮。
- 总对局并发最多120；训练每条16 ranks×local batch 2，global batch 32；开启`numactl --interleave=all`。数值数组压低缓存开销，但不宣称任意后续规模都不会触发资源问题；失败按线路隔离并保留证据。
- 每局结束即无损zstd归档；同时预估采样＋比较＋复核＋最终归档、活跃原文峰值和50GiB余量。MIX、模型、优化器、原始回放、日志不进入Git。
- **26日05:00请求结束训练，最多等到06:00让当前并行轮落盘；随后固定池与交叉评测，接管收尾的评测截止11:15，所有实验最晚11:25进入强制清理，11:30前释放进程。** 若原任务先达到轮数/对局预算或各线停止，就提前完成与释放。

定位：`jobs/commander-holiday-20260924`，tmux`warbook-holiday-24`；源码树`jobs/commander-holiday-20260924-src`不可热改。私有`work/server-feasibility/active-job.json.commanderHoliday`保存plan、state、exit、日志路径和计划SHA。

## 24日临时缩短预算

本次调整解决服务器需要交还用户独占的问题。首轮尚不能证明收益已递减：主线PPO在三固定控制上为10→17/48、13→13/48，BC为18→7/48、10→1/48，压力PPO为13→14/48、7→11/48。不同臂各自重新抽取未控出生的实局，不将这些分数当作配对效应或长期收敛证据。缩短后仍保留六线、段级比较及完整最终比较；减少的是后段更新机会，不能预报完全没有实力损失。

旧驱动只在启动时读取日期，因此直接改JSON不会改变在途截止时间。保留原训练进程、冻结源与原始计划，另用`analysis/experiment_deadline.py`监督这一专用工作树。原计划中的28日日期是历史配置，**实际资源边界以上述26日时间为准**。

监督器先向已核对PID/启动身份的驱动发送SIGINT，让正在运行的并行工作落盘；宽限结束后，按专用工作目录及其进程后代清理，包括另起会话的训练/游戏进程。不会按Python、Node或用户的全局进程名批量清理。之后由`commander_night.py --finalize-from`在新目录复用原冻结游戏源，只读取初始、已保留、最新校验通过及段级参考模型，维持原地图、对手、贪心方式和评测轮数。未被主摘要收取的`profile-complete.json`也会恢复；未校验的半写模型不会选入。旧权重、Adam、完整局及中断尝试均保留。

监督和收尾源`7d3e325`，独立tmux`warbook-holiday-deadline-24`，记录`jobs/commander-holiday-deadline-20260924`；需要额外的`analysis/runtime-requirements.txt`。释放结果看该目录`state.json`中的`phase=released`、`releasedAt`和空`remainingPids`；如果接管了收尾，最终矩阵位于其`finalization/`。监督器结束不会继续安排新训练，不依赖笔记本在线。

**24日17:13已登记真实驱动PID和启动时间，并核对四个新时间边界；原计划SHA与冻结游戏源不变。** 16项调度检查通过；服务器2项真实进程检查覆盖独立会话子进程回收、邻接任务保留及卡住评测的硬截止。小型实局接管测试18次尝试中13场有完成收据，3场采样和2场交叉评测被时间边界中断；释放记录为空残留，失败与中断没有删除。随后另用足够收尾时间完成10场固定池/交叉评测：2W、6L、2U、0E，脚本退出0，监督器自动释放全部测试进程。中断前后初始模型及Adam哈希一致。这些是停机/收尾验证，不用于策略强度结论。

## 已验收的启动链

新版执行器92局预检全部完成、0E、退出0，覆盖共享初始数据、BC/PPO、段级复核、均衡final与交叉矩阵；贪心评测开关未改变PPO采样，实际贪心日志被PPO明确拒绝。初始化/冻结失败和边界进入收尾、失败尝试计数、段级停止均有针对性检查。15项调度、7项序列、7项存储及此前200项Node/类型检查通过。

长期任务六份初始化已验证：主线PPO冷启动、压力PPO恢复Adam；主线首轮八步PPO已导出，压力新完整局正在产生，未见启动错误。长期强度结果尚未产生，玩家默认没有晋升。

Pro第4次协议复核已完成；已本地核对并修复采样/评测开关耦合、冻结异常绕过收尾、跨阶段归档少预留、失败尝试漏计和最终覆盖不均。咨询不替代真实预检；云端目前没有待答请求。
