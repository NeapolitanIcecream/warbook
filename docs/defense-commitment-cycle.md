# 0.1.9 后续：持续交战与两路守备

2026-09-17，用户要求试推进一版。依据 [独立回放与玩家对照](replay-review-0.1.9.md)：共享守备点变化会排除仍可射击的敌人，导致整队往返。本轮先做防守调度，侦察、机会反击与进攻集群属于后续步骤。

## 干预与检查（执行前固定）

1. A：近处敌人不因待机点变化被排除；保持仍可交战的原目标，允许显式紧急改派。
2. B：在 A 上利用现有任务归属，按可见侵入者组织最多两组守备。按威胁与距离分配闲置兵力，保持正在交战的成员；关键建筑急危时允许抽调。生产、正常反击门槛和车辆近距碾压冻结。
3. 首轮三版（0.1.9 / A / B）× 两图（短图、小镇）× 两对手（冻结 0.1.7、Supalosa）× 两轮，共 24 次普通完整新局。先检查行为；不假造配对种子或出生位，不以这一小批总胜率独立证明组件收益。
4. 有实际改善的候选再与 0.1.9 在四图同池（旧阵地、counter、Supalosa）各两轮，共 48 次新局。异常和截断全部保留；行为失败先修，再重新固定版本和预算。旧回放用于分析方法反例，不计新策略胜负。

验收看持续交战是否被无意义打断、另一侧是否获得合理支援，以及必要改派是否仍可发生。回放检查实际姿态与开火、阵亡，峰值/任务人数不能单独作为通过证据。沿用简短摘要与 computer use，必要时补最小状态片段。

## 已有对照

开工首先运行冻结 0.1.9 对 Supalosa：`runs/defense-commitment-start`，短图起点 1，tick 16287 正常获胜，墙钟 2.67 秒。这只确认当前真实运行可用；与此前展示回放不是相同随机局。

## 首轮结果与修正

- A `8ace5fe`，B `6bb6456`；`defense-commitment-screen` 的 24 次全部正常，0.1.9 / A / B 为 8/8、8/8、7/8。B 在短图对旧 0.1.7 起点 1 败北。没有据此宣称分兵提高胜率。
- 首轮 B 对 Supalosa 两次 actor 均在起点 0（交换执行槽位不控制出生）。执行前另固定 `defense-commitment-approaches` 八次覆盖检查，八次正常胜出，actor 起点 1 六次、起点 0 两次。
- 对首份起点 1 的 B 回放复查：4:37 附近出现 5/1、6/0 的守备分配；6:32 后兵力降到三人，所选单路和任务标识仍会反复变化。Computer use 检查 4:01、5:09、5:51 的实际站位/接敌；另查看起点 0 的 5:07、6:31。B 的九名步兵损失和最终胜利分别保留，不能把较少部署取消解释为整体防守已解决。
- C `44521bf`：保留所有可见来向的任务身份；人数不足以覆盖可见威胁时保持当前一路，仍允许急危重分配；不把一名尚未接敌的闲兵派成第二队。`defense-commitment-recheck` 16 次全正常，A 8/8、C 7/8；C 再有短图起点 1 对旧 0.1.7 的失败，保留为已知反例。
- 已做简短结构检查：分配状态集中在一个游戏内 helper，复用任务归属和指令提交；未加入通用调度框架、全图真值或新依赖。现有运行时间不能作严格性能增益比较。

接着对 C 和 0.1.9 执行原先固定的 48 局四图同池检查，并复查 C 的短图行为；这一步决定保留方式，不提前晋升。

## 四图结果与候选冻结

`defense-commitment-pool` 的 48 次全部正常结束，严格批次分析核对启动台账、冻结身份与原胜负规则通过。

| 版本 | 旧阵地 | counter | Supalosa | 总计 |
| --- | --- | --- | --- | --- |
| 0.1.9 | 4/8 | 7/8 | 7/8 | 18/24 |
| C | 6/8 | 8/8 | 8/8 | 22/24 |

小镇 / 短图 / 城市 / mp08：C 为 5/6、6/6、6/6、5/6，0.1.9 为 4/6、5/6、4/6、5/6。城市 C 出生 1 五次、出生 0 一次，旧版各三次；小镇、短图分布也不同，mp08 两者均各三次。这是已用地图上的开发比较，不足以证明组件独立收益或真人水平。

保留 C 为 **0.1.10 试验版**，先通过候选入口提供，默认 0.1.9 保留。理由：同池结果支持继续使用，但 C 的起点 1 对旧进攻 AI 失败，以及对 Supalosa 部分局九名步兵损失仍未解决。新的分组需求只按可见兵力做粗略估计；持续阵型、抗碾压、侦察及机会反击尚未实现，不能宣称成熟防守。

发布来源 `f70eb3b06441134ddef64e26f6f50679f3b7ea79`，只比 C 多版本/入口/记录变化；Node `18ed0792e57bbd4873105c843b63bd369544e97d261ea4561c53da378a354168`，GUI `efe58bafefe3c6564abb4bc2e0aa63208446e25d97ad5a395095afce3969f2eb`。两个完整发布检查均胜 Supalosa，10,090 次同观察指令比较全部一致。

所选当前版本回放 `defense-commitment-release-check/mp06t2.map/supalosa/0-release`：actor 起点 0，17:38 正常获胜；106/106 快照和终局一致；三名步兵、一辆坦克摧毁事件，没有十秒内无火力便解除的部署片段。记录版本与 GUI 同为 0.1.10。两路出生 1 的行为仍以先前 C 记录检查：保持一路、再转移及有限分组确实发生，但仍有九名步兵损失，不能只展示低损失样本。

截至发布标签检查共 99 次 headless 启动，全部正常，分属启动检查、筛查、覆盖、同池与标签验证，不合并成一项胜率。67 项 TypeScript 测试、类型检查和构建通过。

## 玩家与入口验证

- 候选 GUI 会话 `e21be464-35a1-43e5-a7ad-9354f3536597`：美国小镇、速度 4、美国、10000、零起始单位。实际展开基地车、排队并放置电厂，正常结束于 tick 12163；单方败北、状态 Ended，结算显示 13:40。结算后重开，再主动退出于 tick 1449；后者状态 Started、双方未败北，未计正常胜负。无运行/提交错误。记录 `runs/player/defense-commitment-browser-check.json`。
- 正式离线服务 `127.0.0.1:8642`，PID **89005**。默认仍为 0.1.9 `3d784b5a…`，`/challenge/` 为 0.1.10 `efe58baf…`，首页与实际候选客户端均显示正确版本；主包、候选包、新回放 HTTP 内容哈希匹配，见 `runs/player/defense-commitment-release-check.json`。
- 首页回放切至上述 17:38 发布检查，SHA `37e156ba51ca89fda45498752bdbdaf5361ca423ef1895fafb8839e22cf521b0`；computer use 检查加载和 5:57 的防线。还在临时入口查看 C 对旧主线败局的 9:47 两侧基地，未声称完整肉眼看过全部交战。终局与损失结论由原回放/日志支持。
- 本轮临时页面 21–25 已关闭，8643 临时服务已停止，仅保留维护入口。没有新依赖、引擎修改或资源上传。

后续先让独立侦察为两路预警与反击提供信息，并修正打退进攻后的机会判断；当前分兵与步兵接敌损耗仍需随这些反例继续检查。本轮没有启动后台研发或自动化。
