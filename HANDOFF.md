# 执行 Agent 开工交接

> **首次实施于 2026-09-15 14:09 启动，首次阶段截止为当天 17:30；后续迭代来自用户继续授权。当前实际结果与续接事项见 [执行进展](docs/progress.md)。以下“未启动”等表述保留为规划时点记录。用户要求不复用废弃本机代码，完全本地推进。**

## 当前入口：2026-09-18，0.1.11 已设为默认

本轮沿 0.1.9 的具体反馈持续修复，当前行为源 `7a42d2a`；Node `e13babc0…`，GUI `35225d57…`。正式离线服务 `http://127.0.0.1:8642/`，PID 42115；默认 **0.1.11**，`/challenge/` 保留 **0.1.10** 对照。下面旧版本与昨日截止都是历史记录。

- 已实现独立军犬与四起点分路、机会反击及行军重估、GI 射程/部署衔接、可通行驻防和建筑通道、按需回防、独立恢复与原生 Scatter 让路、矿场实际出矿路线。不要恢复全军等待少数撤退单位、任意直线集合点或永久固定部署。
- 最终四图同池 48 局全部正常，新旧均 20/24；新版城市 3/6、旧版 6/6，出生分布不同，不能宣称整体实力提高。全部四份新版败局与展示局共 1,037 份原快照和终局核对通过，98 项测试与类型检查通过。具体缺陷和次生问题的修复证据、负结果见 [0.1.11 验收](docs/closure-019.md#2026-09-18-0111-发布验收)。
- 当前回放 `runs/closure-release-pool/mp06t2.map/supalosa/0-closure-candidate`：16:54 胜 Supalosa，6:30 看防守，7:16 开始反击，8:56 后敌建筑实际掉血；仍损失电厂、步兵与坦克。GUI 已通过展开、电厂建造放置、正常结算、重开与主动退出；HTTP 包/回放哈希已核对，记录 `runs/player/closure-release-check.json`。
- **引擎事实修正：实时 `findPath` 会修改共享寻路缓存，曾造成原始回放分歧。** 当前规划用独立 ngraph 图，只读己方已探索地形；不得直接恢复实时 SDK 寻路查询。旧分歧录像原样保留，不算验证通过，见 [环境审计](docs/environment-audit.md#2026-09-18-运行补充规划寻路查询并非无副作用)。
- 城市野战、攻击风险估计和守势攻坚仍有能力短板。后续从记录中的具体败局推进，不重新泛化预研、不把每次等待判成控制故障、不把本轮持平比分包装为胜率提升。临时游戏、回放页面及 8643 服务已清理。

## 2026-09-18 恢复推进时记录

用户授权继续；先复跑昨日候选并修敌方产能/来援估计，进展与具体反例见 [持续修复记录](docs/closure-019.md#2026-09-18-0915-恢复推进)。以下截止是昨日状态，不是当前实施限制。

## 2026-09-17 检查点：按用户 17:30 截止要求收尾

2026-09-17 用户随后要求本期在 17:30 前收尾，现已停止新增实验与功能。实现证据提交 `fcad322`；本轮 107 次真实引擎执行均正常，最近 24 局两版各 10/12，仍存在反例，未宣布整批闭环或发布新版本。下一轮优先修未知产能与敌方来援低估，再做按需回防、四起点侦察、矿场路径与集群复查。未接线、未测试的矿场草稿保存在忽略目录 `work/closure-019/refinery-site-draft.ts`。

用户随后要求持续迭代，直到这批问题全部修完，不以发布一个候选作为终点。当前工作分支 `codex/close-019`，范围、验证和最新中间证据见 [持续修复记录](docs/closure-019.md)。已推进独立军犬、短暂不可用补产修正、实际三维射程和部署后开火；继续做机会反击、坦克实际协同、经济与矿场疑点，以及分析方法的对应检查。默认入口仍是下述已验证版本，尚未将中间代码切入玩家入口。

## 2026-09-17 入口：0.1.10 候选，默认 0.1.9 保留

**2026-09-17 14:57**：正式离线入口 8642/PID 89005，默认 0.1.9 GUI `3d784b5a…`；`/challenge/` 已改为 0.1.10 GUI `efe58baf…`，发布来源 `f70eb3b`，Node `18ed0792…`。本轮 [持续交战与两路守备](docs/defense-commitment-cycle.md) 记录预算、候选、负结果和实际玩家检查。

用户指出两路拉扯、缺少侦察和错失反击机会。本次先修防守：保留近处有效交战，有限分配两组步兵，兵力不足时集中一路，急危建筑允许抽调。A/B/C 各自冻结；最终同池四图 C 22/24、0.1.9 为 18/24，48 次全部正常，但出生分布不同，且保留了对旧 0.1.7 失败与部分局九名步兵损失，因此先提供试验版。

GUI 通过建造、正常结算、重开与主动退出。首页新回放为 0.1.10 短图出生 0、17:38 胜 Supalosa：`runs/defense-commitment-release-check/mp06t2.map/supalosa/0-release`。106 份快照和终局匹配。99 次 headless 全正常，67 项测试通过；临时页面和 8643 服务已清理。

接下来按已讨论顺序推进独立军犬侦察、机会反击，同时继续检查步兵接敌损耗与有限分兵；不要把胜局或峰值解释为防守质量已解决。新的 `analyze:encounter` 会指出短时无火力部署取消与损失时支援距离，再用 computer use 看相应片段。策略 `threats` 只包含可见接触，`engagement.interrupt` 是显式紧急改派；生产与进攻门槛本轮未变。

## 0.1.9 发布时记录

**2026-09-17 12:03 已设为默认。** 正式离线入口 8642/PID 75850，默认 GUI `3d784b5a…`，旧版对照 `/challenge/` 为冻结 0.1.7 `632848aa…`。行为来源 `c1bfcff`，Node `7bbe43fc…`；GUI 构建提交 `dd8e0ae` 只多实验计划。完整过程见 [主动防守实施](docs/active-defense-cycle.md)。

修复了步兵不接战、护援遗漏生产建筑、部署与目标脱节、无效归队方向等问题；增加已探索路径的预备位置和近处车辆碾压支援。反击继续使用完整目标的原生命令。四图同池新版 39/48、0.1.7 为 25/48，96 次全正常。短图 12/12，城市 7/12；城市和 mp08 对旧阵地各 1/4 是下一轮具体反例，不再重做泛化预研。

默认回放 `runs/active-defense-release-check/mp06t2.map/supalosa/0-release`：新版 17:12 胜 Supalosa。4:30 观察防守参与，13:19 后看反击。完整重放、104 份快照和终局已核对。GUI 完成建造、正常结算、重开与主动退出；当前包、对照包及回放 HTTP 哈希通过。

先读当前进展和相关反例，再执行。简短行为摘要 → computer use 看异常时段 → 必要日志/源码核实；`npm run analyze:encounter` 默认不输出逐帧，必要时用 `--window`。保持战略/战术/生产和 API 桥接边界；别恢复固定部署不管、不可达直线短途点、或只统计到岗数量的验收。

旧移动原型保留冻结研究分支：`4ab2073` 只修改完整目的地；八局检查中修正版的四局均在出击前败北，不能视为已确认收益；当前代码仍未恢复该原型。其他历史记录如下，均不替代最新状态。

## 上一阶段：0.1.8 阵地反击挑战者

**2026-09-16 16:44 已更新正式入口。** 默认主线仍为冻结 0.1.7（图形 `632848aa…`），`/challenge/` 已切到阵地路线 bastion 0.1.8，最终来源 `b3a1f7c`，Node `5287b0b2…`，图形 `b377f147…`；正式离线服务 8642/PID 49183。完整实验与负结果见 [阵地防守与持续成军](docs/defense-cycle.md)。

新增主力/守备/预备/汇合任务、单位归属与切换、部署守军、有限工事和近区协防。正常六车反击，观察到近区装甲压力降低时可由四车反击，之后援军至少积累四辆坦克再组成汇合批次。攻击阶段仍用原局部战斗，不能称为已完成持续队形、T 优或残血轮换。

阵地路线四图对 0.1.7 为 12/16、counter 13/16、Supalosa 3/16；总体 28/48 对主线 38/48，保留为专门挑战者，不替换默认主线。先行包修复全灭后的援军回收，四局正常、18,823 次同观察指令无差异；最终包对齐并强制执行显式碾压权限，两局正常、12,183 次同观察指令无差异，48 项测试通过。部署/工事消融未确认独立增益，短图各组合均出生位 0 全胜、1 全负，不能把不同起点覆盖下的总分差当作组件收益。

主线试验：移动/绕行版本 0/8（3 上限），已移除当前实现，`4f107e7` 可重建；恢复原局部战斗的 `cohort-local` 6/8 对同批主线 6/8，先成军再扩矿候选 3/8 对 6/8，均未晋升。不要继续微调同一大队的中心/绕圈参数；上限样本有 53/60 辆坦克，规模、射击与攻坚控制仍不可靠。

下一项优先用短图起点 1 反例定位防线覆盖、早期步兵压力和第一矿场/重工保护，再推进有限小队与实际火力窗口。`bastion-release-check/mp06t2.map/supalosa/0-release` 在 6000 已无矿场、仅一辆坦克，对方可见八名 E1；不要统一归为空军问题。旧 counter 仍保留冻结包。

先行图形包完成三局检查（两次正常结算、一次建造后退出）；最终图形包再次通过进入、展开、建造、主动退出与重开，两次主动退出单独记录。观战为最终阵地包对 0.1.7 的正常胜局，17967 tick，原回放核对通过。所有本轮测试实例与候选服务均已释放，发布检查见 `runs/player/bastion-release-check.json`；资源及本地配置不进 Git。

## 上一阶段：0.1.7 生产记账修正

**2026-09-16 12:54 已发布 0.1.7。** 仅修正生产承诺记账，战略与战术保持原样。矿场队列、施工中矿场和正在造的矿车进入数量计算，取消/毁坏后重新计算。最终来源 `8326311`，Node `377302a5…`，图形包 `632848aa…`；正式服务 8642/PID 26756。四图固定池候选 53/72、旧版 52/72，144 局均正常；城市补查候选 11/12、旧版 9/12（另 2 负、1 上限），分开报告，不宣称综合增益。详见 [生产记账与实战](docs/production-cycle.md)。

玩家最终包已通过正常结算、重开、建造、主动退出。观战改为 `runs/production-demo`：0.1.7 WarbookRed 对 0.1.6 WarbookBlue，新版本局败北，原回放与终局核对通过。当前新策略仍可能因重建矿场得到第五辆免费矿车，这不是车辆工厂重复购买。

**最新玩家反馈已调整下一轮优先级：** 先建立能利用步兵部署、有限工事和短距离增援的守势对手，再持续组织主力、待汇合增援与守家兵力，随后单独比较集群机动/承伤轮换，经济和产能服务于这些任务。完整经验、已核对缺口和顺序见 [0.1.7 玩家反馈](docs/player-feedback-0.1.7.md)。本次已验证灰熊/犀牛 Move 下能移动自动开火、大兵能部署；AttackMove 也可能停步，不能只按名字选动作。六次正式机制试验不计竞技胜负，正式策略仍为 0.1.7。

扩矿与连续补兵仍有待解决，但不能只让单位更快地逐辆送入前线。不要恢复无效的群体命令批处理或曾导致回家的严格全军聚拢。旧版上限局 `production-urban-check/mp29u2.map/supalosa/1-published` 仍有五辆矿车和大量坦克，原因未归定，不能套用此前无矿车僵局的解释。继续维护完全本地入口，不复用废弃项目代码。

## 上一阶段：0.1.6 分层实现

**战略/战术分层已实施：** 用户同意后，已抽出战略、战术、生产控制和任务协调器，实际结果见 [分层实现记录](docs/layered-control.md)。默认继续用原战术，最终源码 `e3e338f`、Node 包 `821ead10…`；最终 8 局均正常结束，36,403 次同观察指令比较无差异。原生群体行军试验两边各 6/8，关键掉队反例仍在，未晋升。影子零差异用于行为保持重构，后续真正修改战术时用完整局对照，不要求新战术继续输出旧指令。当前只有一个主力任务及一个生产任务；RPA、异步高层推理与多小队调度仍待独立适配。玩家最终发布状态见 [当前进展](docs/progress.md)。

**2026-09-16 0.1.5 更新：** 本轮已推进开局集结、实际出厂和伤亡后进度保持，最终策略为 `factory-exit`，冻结来源 `d73f7855419be3e53013d2e94c9d5ba1c307d574`，Node 包 `1c722c3d…`。三图固定池 44/54 对 M0 30/54；新增 `mp08t2.map` 两者 12/24。`formed` 的严格靠拢曾造成城市 Supalosa 明显退化，未采用；不要恢复这项条件或合并不同版本的成绩。版本发布及玩家最终验收状态见 [当前进展](docs/progress.md)，完整反例与计划见 [本轮记录](docs/assembly-cycle.md)。下一项优先研究已观察的开局经济投入与补兵空档；新图已有结果，后续视为暴露来源。

**2026-09-16 分析改进补充：** 已补齐可复算批次报告、验证过的密集回放时间线和两因素冻结对照，见 [分析工具与验收](docs/analysis-tools.md)。复用现成命令继续，不重建分析平台。新证据区分了两类首车失败的时间条件，并显示 counter 的集结目标和显式接战过滤需要分开研究；小样本组合 4/4 未晋升。默认 M0 与可选 counter 图形包保持不变。

首期已交付 Warbook 0.1.4、玩家对战入口和一键完整回放。共同演化首轮已于 21:07 获准执行：双方独立冻结包、真实挑战试跑和固定池比较已经完成，详细证据及图形入口验收状态见 [当前进展](docs/progress.md)。官方 AI 与 npm Supalosa 属于同源构建，不计成两个独立策略家族。

续接时读 AGENTS、本节、[当前进展](docs/progress.md) 和 [专项方案](docs/opponent-pool.md)。不用重做版本接入或重新寻找首局：`scripts/build-bot.ts` 从固定 Git 构建不可变 Bot，runner 支持双方 release 与槽位交换；`.env` 分别固定默认和实验图形包。默认仍为 M0，counter 在 48 场固定开发比较中 17/24 胜，M0 为 15/24，尚无正式增益确认。

保留 counter 的守势集中行为及冻结版本。骚扰三次变体未形成有效经济压力；事后 regroup 在援军赶到前仍会损失首车，也出现进退反复，均未晋升。下一项应围绕出发时机、局部兵力集中与机会成本做有判别力的对照；准备独立新来源时另行预留，已有三图仍是开发资产。不要通过继续调骚扰阈值或刷同源对手数量代替新证据。

以下内容是首轮开工前的交接归档，保留用于追溯，不替代本节与当前进展。

本交接针对规划基线 `f088f8a` 之后的收尾版本。**规划已经足够；收到用户“接手并开工”的指令后，直接实施。** 文档里“规划时尚未启动”的表述是历史状态，不是实施禁令，也不要求重新确认已授权工作。

## 最短入口

先读 [AGENTS.md](AGENTS.md) 和本页，然后按需要查 [接口契约](docs/interface-contract.md) 的观察、命令、调度与结束部分。时间/目标以 [战略规划](docs/strategy.md) 为准；其余文档是按问题使用的参考，不要求全部读完或检查全通过才开始。

**第一项工作：用固定引擎和可用 MIX 资源跑出真实敌对对局，同时查明用户能亲自对战的入口。** 不先建设 RL 平台、任意分叉、完整场景库或新的任务管理系统。

## 首轮开工前的状态（历史）

| 已有 | 尚无 |
| --- | --- |
| API `0.79.0` / 引擎 `0.84` 的静态审计与来源哈希 | 实际游戏资源初始化、自己的 bot 与根目录运行工程 |
| 两份公开真人 replay 已由官方 parser 解析 | `loadReplay` 仿真、合法观察重建、动作成功标签 |
| 提取 `orderUnits` 方法的 5 个 mock 分支结果 | 对应真实地图/游戏行为的验证 |
| 固定 Supalosa 深路径模块加载/构造成功 | 其游戏生命周期、敌对对局或公平信息条件兼容 |
| 可复算统计分析、实验模板与有针对性的实验菜单 | B0、实战成绩、吞吐估计、训练、已验证试玩 URL |

仓库目前是本地 Git 仓库，收尾检查时没有配置 remote；交接本地目录即可，不需为开工先发布远程。实现 Agent 启动后更新 README/roadmap 的实际状态，保留旧探针的证据范围。

## 外部输入与本机缓存

- **MIX 资源路径：未确认。** 收尾检查的 shell 未设置 `MIX_DIR`，仓库根目录没有 `.env*` 文件。这不证明用户没有资源；优先复用用户已经提供的位置，确实缺少时一次询问。资源缺失阻塞真实首局，不以更多文档代替运行。
- **玩家在线入口：未确认。** 当前 shell 未设置 `XWOL_API_KEY`、`XWOL_API_URL`、`CLIENT_URL`。现成候选是外部 Node Bot + 官方浏览器客户端；需实际有效的 Bot API 凭证、端点与加入链路。普通玩家账户不自动具有该 API 能力。只确认必要的配置位置/身份授权，不把密钥写入 Git。
- **持续运行和资源上限：由执行任务现场状态决定。** 本仓库没有开启后台 Agent、自动化或72小时运行。复用既有授权与运行机制，不为此先建平台，不擅自扩大付费资源使用。
- `work/` 中的安装、下载样本和原始日志是可丢弃缓存；不同机器或新 checkout 可以没有这些文件。恢复方法与固定来源见 [回放探针](docs/replay-data-probe.md)、[对手调查](docs/opponents-and-data.md)、`analysis/` 下两套分析锁文件。它们不是 bot 项目启动工程。
- 关键来源、锁文件、公开 replay URL、哈希和聚合证据均已跟踪；不需要原聊天、Downloads 中的 Pro 原文件或这台机器上的旧项目。参考原稿已原样归档到仓库。

在线凭证只阻塞相应玩家通路，不能让可推进的 headless 实现和自动实验停住。浏览器内集成或自建服务不是已验证的廉价备选，避免为了绕开一个访问前提直接大改引擎。

## 首个真实运行的做法

1. 记录实际启动时刻、当前 Git 状态和可用资源位置。按照已审计 API 精确建立自己的最小运行工程；分析探针的 peer bypass 不应未经判断照搬为正式依赖策略。
2. 初始化普通可用地图，先采用简单同步控制。候选是 Americans 镜像、`mp03t4.map`；实际加载不符时换可枚举、可解释的普通地图，不凭名称断言公平性。完整试跑选项见 [前期决定](docs/preimplementation-decisions.md)。
3. 首轮只贯通展开、经济/生产、建筑放置、移动/交战的必要动作；driver 统一决定何时观察和提交，分别记录请求与后置效果。可以先用起始军队贯通引擎，再补齐自己的完整玩家。
4. 保存实际停止原因、defeat 观察、异常/截断和回放。不能确认正常胜负就明确标未决；先观看并解释一场真实运行，不先建立通用胜负服务。
5. 同时准备用户入口，验证“打开 → 加入 → 实时操作 → 结束/退出 → 再开”。用户只试玩与表达体验；Agent负责进程、配置、日志和技术诊断。回放不是可玩版本。
6. 得到第一份真实证据后，选最高影响的问题实施下一项改进。只有具体实验需要时，才补相关机制检查、对手适配或性能优化。

首个6–12小时是推进与校准窗口，24/72小时是滚动检查目标，均不是最低必须消耗的时长或已经保证的交付时间。提前完成立即推进；未达则报告具体原因与下一步，不把任务交还用户来做技术决策。

## 开工时记住这些已查问题

| 已知边界 | 直接影响 |
| --- | --- |
| `isFinished()` 同时可能表示 turn-manager 错误 | 分开停止原因与比赛结果；不能按一个布尔值记胜利 |
| 全局查询/事件可暴露隐藏状态；`api-shroud-v0` 仅工程条件 | 用显式观察边界，不将工程试跑成绩升级为公平竞赛证据 |
| Bot 回调不会 await 异步结果 | 统一 driver；首版同步；以后拒绝过期/跨局结果自行下令 |
| 单位指令可能被改派或部分入队 | void 返回、单位移动、选择动作入队都不是预期命令成功证明 |
| 没有公开 seed/fork；sandbox 随机 helper 不等于受控种子 | 正常新对局可先比较，记录实际起点/批次；不假造精确配对 |
| Supalosa 版本范围不含目标 API、裸入口缺失；作者1v1示例请求互相结盟 | 固定版本和深路径，核对单份 API 与空结盟名单；load通过不代表生命周期通过 |

详细来源在 [环境审计](docs/environment-audit.md)、[接口契约](docs/interface-contract.md) 与 [对手调查](docs/opponents-and-data.md)。固定版本未变、没有新反证时，不为交接重做这些调查、mock、解析、加载探针或统计算例。

## 交付与续接

保持一个可运行/可玩的稳定版本，候选独立验证后替换。约每3轮实质迭代或遇到明显耦合/性能问题时检查，确有需要就自主暂停相关功能做最小有效改善，再恢复主线。用户不反馈时继续自动研发，不将沉默当认可。

利用已有进展记录保存当前版本、基线/候选、实际已验证结果、下一步与进行中进程/产物路径即可，不新增仪表盘或状态框架。首个检查点提交真实记录、当前可体验程度、关键缺口，以及基于实测的未来24/72小时预测。主指标约 +10pp 和独立证据要求保持不变，不为赶检查点改分母、改对手或制造成功。

交接收尾已在仅导出 Git 跟踪文件、不携带 `work/` 或 `node_modules/` 的副本中检查：17份 Markdown、10份 JSON、129个本地链接均可读取/解析且无缺失文件引用；分析依赖声明与锁文件一致，Pro原稿哈希保持不变。这证明文档与已有分析证据可随仓库移交，不证明游戏已经能启动；没有重跑引擎或既有探针。

可供用户交给执行 Agent 的启动文字：

> 请接手本仓库并开始自动化研发。阅读 AGENTS.md 和 HANDOFF.md 后直接实施，按 docs/strategy.md 自主负责技术、实验、重构、性能与版本维护。我只作为用户/玩家检验效果。优先取得真实敌对运行并打通可亲自对战的入口；复用已有预研，必要资源确实缺失时一次说明，其他可推进工作继续。6–12小时和24/72小时是校准与检查目标，提前完成就推进；不要停在重新规划，也不要把回放或未验证结果当作可玩交付。
