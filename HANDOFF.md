# 当前研发交接

2026-09-25，假期任务已提前完成并释放服务器。先读AGENTS、README和本页；用户授权Agent负责研发、实验、重构与资源维护，用户只试玩反馈。不要复用废弃本机项目或重做泛化预研。

## 最近实验：已完成，当前没有活动实验

**25日04:54:37正常结束并释放资源，提前于26日的交还期限。** 用户需要独占服务器，不自动重启实验。25日11:41快速核对208份批次摘要：28,096场，其中4,242场游戏时长上限、0程序错误；主任务和监督器均退出0，无残留进程。main-ppo-47更新24轮，其余五线12轮；均触发连续两段未达增益门槛的停止规则。尚未做逐局产物/回放审计。[协议与快速结果](docs/commander-holiday.md#25日快速检查已提前完成)。

- 冻结源：`0058aa088adc4046c2079f67e466f8abcb2eb762`。服务器与准确路径见私有`work/server-feasibility/active-job.json.commanderHoliday`。
- 作业`jobs/commander-holiday-20260924`，源码`jobs/commander-holiday-20260924-src`；原tmux **warbook-holiday-24**及截止监督器均已退出。不要重复启动，不改写冻结源码或计划。
- 六线：main-bc / main-ppo / pressure-ppo，各47/83。每个主线种子的BC/PPO同一强C0权重起点，首批共享已有128局；PPO不使用查询标签。主线PPO冷启动Adam，压力PPO续接日间Adam，其后必须连续。
- 主线BC：T=1监督、600更新，部署T=.01；主线PPO：T=.01、8更新、lr5e-6；压力PPO：T=.2、8更新、lr1e-4。完整v2战略权限与固定战术保持。
- 当前对方路线与Supalosa、对方0.1.16规则、旧强MC83共同组成训练池；每轮冻结。current持续更新，retained保存；每六轮在四固定控制上复核，连续两段无规定增益则停止该线更新，仍保留为对手。
- 最多40轮/8万次新对局尝试，包括失败、中断、复核和最终矩阵；共享旧局只计使用量。总对局并发120，训练16 ranks×local batch 2，NUMA交错分配；最近两批缓存有界，逐局zstd归档。
- **26日05:00请求停止，06:00结束训练宽限；接管的最终评测截止11:15，11:25强制清理，11:30前释放。** 原JSON的28日边界保留作历史，不是当前实际资源期限。固定池和交叉协议不变，若原任务更早完成则提前释放。
- 独立监督器源`7d3e325`，tmux **warbook-holiday-deadline-24**，目录`jobs/commander-holiday-deadline-20260924`。它保持原训练进程运行，在截止时按专用工作目录清理，随后用原游戏源和已落盘/校验的模型进行最终评测；不重新训练、不热改原源码或计划。私有定位见`active-job.json.commanderHoliday.resourceDeadline`。
- **17:13监督器已独立启动并核对PID身份/日期；临时预检全部退出。** 16项调度、2项真实进程检查通过；实局截止测试保留5次主动中断，随后10场完整收尾评测2W6L2U0E、退出0、残留进程为空。完整来源在假期协议，不能把这组管线验收混作训练收益。
- 启动核验：92局完整预检0E、退出0，包含更新、段级复核、最终及交叉矩阵；真实贪心日志被PPO拒绝。长任务六初始化通过，主线首轮PPO已经导出，压力新对局持续产生；强度结论尚未产生。

## 提前释放与收取顺序

1. 先检查监督目录的state/exit：`released`、`releasedAt`和空`remainingPids`是资源释放记录。若有`finalization/`，那里是接管后的最终矩阵；否则读原实验最终结果。原驱动因操作员中断退出不能直接归为训练故障。`complete`仅表示脚本结束，不代表策略变强；明确预算/停滞/错误停止原因，不在资源交还后自动启动新任务。
2. 按唯一游戏目录核对各summary、manifest/result、attempt-started与模型SHA。区分实际新尝试、完成对局、重复使用量、BC教师查询、纯PPO及最终评测。
3. 看每条路线的initial、retained、latest、confirmed与段级曲线，按对手分开。不要只挑最高点，不把动态同代池与最终四控制池直接相减。
4. 根据异常摘要选必要回放，直接看战场并核对最小日志；检查后关闭临时实例。再决定新一轮实验，不原样恢复已经过截止的任务。

## 今天已经完成的工作

- 日间共同演进3520场已逐局核验0E；两条第二轮BC曾发生训练OOM，不能混算成比赛错误。紧凑缓存将同24局/8697帧峰值5.13→1.70GiB，355万输入元素、4步BC及16rank八步PPO严格一致；NUMA分配已实测。
- 原两条384局/600步BC已用新源补跑完，另与强父模型/PPO做192局，0E。固定三控制主线PPO合计17→22/48，两个种子分别10→11、7→11（各24）；这是小样本正信号，不是持续提升证明。较弱父模型的另一份64局PPO试验退化，两者分别解释。
- 压力short47最终四固定池14→24/64，83为16→16；small没有一致收益，失败与模型已保存，本次活动位置换成主线PPO。
- 两份旧夜间作业104032份日志已无损整理完成。所有临时实验、回放/Pro标签与8643已关闭；只保留维护玩家入口与当前长任务。不要终止无关的`loreley-embed-warmup`。
- 200项Node、类型检查；调度15项、序列7项、存储7项与真实92局预检通过。M4 Pro四真实状态纯策略前向P95 6.8–10.7ms，不含观察、战术与渲染。

## 设计约束与Pro状态

- consult-pro第4次已完整收取并核对，**没有待答请求**。私有`work/consult-pro/strategy-discovery/state.json.fourth`与`reply-04.md`。已修复采样/评测贪心开关耦合、冻结/初始化异常绕过收尾、跨阶段空间少预留、失败尝试漏计及最终覆盖不均；不要重发。
- 完整战略由模型负责，固定战术/原生命令负责执行；不悄悄缩权限或加规则救援。保持主线、压力联合演进与旧强控制，玩家默认不自动晋升。
- 经典法国冰天打法只作人类侧能力审计，不能进入示范、奖励、专门课程或手写完整模板。Americans及若干操作/表示边界见[战略发现审计](docs/strategic-discovery-review.md)，当前不是全部RA2行动空间。
- 消费级MacBook流畅推理是设计基础。所有MIX、凭据、模型、优化器、原始回放、数据和工作脚本留私有目录，不入Git。

## 玩家和历史

稳定入口 **http://127.0.0.1:8642/**，0.1.16；HTTP与固定版本已核对。分支`codex/full-strategy`、远端`NeapolitanIcecream/warbook`。分支文档HEAD与运行源可不同，以冻结SHA为准。

[进展](docs/progress.md)、[计划协调](docs/commander-coordination-cycle.md)、[日间试跑](docs/commander-joint-cycle.md)、[存储](docs/experiment-storage.md)保存详细证据。[整理前交接](https://github.com/NeapolitanIcecream/warbook/blob/b75288efcc0d4d19b005476095a9622e1a8942f5/HANDOFF.md)只作历史，不把旧的“正在运行/等待Pro/尚未开工”当作今天状态。
