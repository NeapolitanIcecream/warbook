# 当前研发交接

2026-09-24，15:15。先读AGENTS与README，再按下面的真实任务状态续接。用户让Agent负责研发、实验、重构与资源维护，用户只试玩反馈；不复用废弃本机项目，不重做泛化预研。

## 今天必须完成的交接

- **今天17:30结束本地工作。25–27为假期，28日09:00收取。** 根据当前试跑结果，在离线前部署并验证有用的独立长任务。假期长期任务目前尚未启动，不能把下面的日间试跑当作已经安排好假期。
- 私有registry中的SSH服务器已恢复。所有计算在服务器运行，任务不依赖笔记本；当前不要重复开同一作业、重启昨日失败作业或改写冻结工作树。
- 私有任务定位以`work/server-feasibility/active-job.json`为入口；实际成绩以各任务的state、逐局summary/manifest/result、模型SHA和完成收据核对。计划局数、根状态、最新模型都不能代替实际结果。

## 活动作业

### 六线联合学习试跑

- 冻结源 **0288f66**；任务`jobs/commander-joint-20260924`，源码`jobs/commander-joint-20260924-src`，tmux **warbook-joint-24**。私有registry项`commanderJointPilot`。
- 两条主线：各自恢复47/83模型，部署T=.01，采集β=0 DAgger（教师仅查询、模型全程操作），旧128锚点＋最近两批实际状态，T=1做600次BC，再导出T=.01。
- 四条压力线：相同T=.2起点，short为8次PPO/1e-4，small为64次PPO/1.25e-5，各47/83两个优化重复。首轮共享已验证192局，后续每条线独立采新数据；共享使用量不计成多份新游戏。
- 每轮冻结当前对方路线，与Supalosa、对方0.1.16规则、强旧MC83组成对手池。主线交替面对两种压力方法；学习中的current持续更新，retained按新同池比较保存。先比较三固定控制，再比较全池，避免弱同伴独占资格。
- 最多两轮/6000场；16:10停止开新轮，16:25切收尾，16:50硬截止。最多120并发对局、96训练rank。最终固定池与两路线交叉对阵分开保存。
- **首轮已完成：** short47旧→新为18→24/64（非同代10→15/48），short83为16→19（8→10）；small47为17→17（10→9），small83为22→18（14→10）。主线47为7→20/64（固定控制5→11/48），83为10→25（6→18）。
- **第二轮：** 主线新128局为43/51胜、0E，但两条384局BC缓存进程被OOM杀死，旧作业已隔离它们，不能恢复或把失败当策略败局。四压力更新完成，开发检查均不高于retained，当前仍在最终固定池和交叉评测；已出的short47 final为初始14、retained18、latest24，各64局，说明不能仅凭一次开发检查停掉学习。
- short首轮两模型与既有8步模型张量逐项相同；逐局zstd归档已在未结束批次中核对完成标记、SHA及原文移除。失败按路线隔离，保留错误，不混作正常败局训练。

### 主线内存修复与独立补跑

- 新冻结 **01a724d**，源码`jobs/commander-memory-20260924-src`。内存缓存改为模型pack本来使用的float32/int32；磁盘原文未改。实际被杀分片24局/8697帧，峰值5.13→1.70GiB，3557896输入元素、4次BC更新完全相同；16rank的8步PPO与既有模型张量一致。numactl interleave=all已验证，不改其他任务的cgroup。
- 活动作业 **warbook-main-recovered-24**，目录`jobs/commander-main-recovered-20260924`：从更强的首轮主线C0模型，使用第二轮各128局实际policy动作做8步PPO(T=.01/lr5e-6)，这两份已训练导出；原两条384局/600步BC按原数据重跑，再与C0/PPO同池192局。正在运行，结果未出，不要重复启动。
- 较弱起点的`commander-main-ppo-probe-20260924`已64局完成0E：47初始2/16→PPO1/16，83为3→0；基础展开/电厂/重工仍存在，不能把它概括成不开基地。两次起点的结果需分开。

### 历史日志无损整理（已完成）

- 状态`jobs/commander-history-archive-20260924.json`已complete；共104032份日志，全部内容核验，现压缩字节15,426,254,623＋18,201,516,853。任务已结束，不重开。
- 仅转码已结束的`operation-night-20260921-v2`与`credit-night-20260922`，保留原始内容SHA、全部模型/回放/结果；不触碰当前训练输入。旧冻结读取器需要原文时用新存储工具restore，不热改旧代码。
- 不要终止无关的`loreley-embed-warmup`会话。

## 今天已得到的结论

详见[本轮联合协议](docs/commander-joint-cycle.md)及[计划协调实验](docs/commander-coordination-cycle.md)。

1. 昨夜实际上17:44首轮即停止，576场、1错误、零PPO；不是充分过夜训练后的失败。SPY伪装武器Infinity进入战斗特征已用原回放复现并修复，调度失败隔离也已验证。
2. 新v3计划修改门控会漏第一座电厂，已停用；同权重全开门只部分恢复，不继续堆门控。
3. 主线加入纯学生失败状态纠错后，同600更新、两贪心重复6/32和9/32；继续旧数据只有0/32和1/32。9胜中8胜非同代。后续校准波动明显，不能挑最高一次当稳定实力。
4. 四条件诊断分开生产与任务。恢复后的577矿车记录，已有采矿任务却选错112→10；剩余102错误中92是没生成采矿任务。因此先继续上游计划纠错，暂不加成员辅助损失。
5. 压力新采样192局73W99L20U。576场更新曲线：起点29/64；8步33/34，16步32/26，32步30/31，64步27/26。8步三控制均19/48、起点14/48，但强历史仍退化，不宣称普遍进步或新打法。
6. curve根任务因主线低温预检退出1，压力576局已独立完成0E；calibration另192局也0E，旧十二模型复查通过。数值检查分别约束概率/hidden/value与联合似然比，原先统一绝对阈值误挡了低温的累计logp；真实概率误差已核对。
7. zstd六局同字节比较79.6→16.1MiB；四真实episode/865决策跨Python、Node与恢复一致。新源逐局归档，空间预算含活跃原文、待生成压缩包和50GiB余量。当前200项Node、类型检查、存储7/序列7/调度8项通过。
8. M4 Pro四真实状态纯策略前向P95 6.8–10.7ms、释放后无tensor增量；不含观察、战术、渲染，不等于图形客户端帧率验收。

## Pro与用户设计约束

- consult-pro第3次已完成；**第4次于15:12提交，正在等待假期协议/执行器窄审查**。私有`work/consult-pro/strategy-discovery/state.json.fourth`，marker`warbook-holiday-protocol-20260924-04`，固定01a724d，brief-04.md。复用现会话，勿重复发送；核对匹配新回复和生成完成状态。相关咨询可云端继续，不能因此拖过17:30。
- 完整战略继续由模型负责，固定战术/原生命令负责执行；不悄悄缩小控制面或加规则救援。保持主线与压力共同演进，Supalosa/冻结历史是控制。
- 经典法国冰天打法只作人类侧能力审计，**不能进示范、奖励、专用课程或手写完整模板**。当前Americans与若干操作/表示边界见[战略发现审计](docs/strategic-discovery-review.md)，不能宣称已覆盖全部RA2。
- 以消费级MacBook流畅运行作设计约束；实际模型与数据留私有目录，不提交Git。

## 玩家与现场资源

稳定入口仍是 **8642 / 0.1.16**，候选没有自动晋升。`/replays`保留既有实验回放；临时回放/Pro标签与8643都已关闭。本地没有当前训练负载，除维护入口外不保留无用实例。

分支`codex/full-strategy`，远端`NeapolitanIcecream/warbook`。继续用Git提交源码与必要证据摘要；所有MIX、密钥、模型、日志、原始回放和工作脚本均不入Git。当前运行源与分支文档HEAD可以不同，以任务冻结SHA为准。

[执行进展](docs/progress.md)保存阶段结果。[本页整理前的历史交接](https://github.com/NeapolitanIcecream/warbook/blob/b75288efcc0d4d19b005476095a9622e1a8942f5/HANDOFF.md)可追溯旧任务，不把其中“正在运行/等待Pro/尚未开工”当作今天状态；离线也可用git show读取该固定版本。
